# Material workflow consolidation

September 8, 2026. Objective: keep ongoing matte/scale production checks usable
as the asset library grows, preserving the evidence behind them.

The maintained `material-and-scale-review.md` is now119 lines instead of304.
It covers brief/reference roles, separate material/camera/scale/alpha/integration
gates, native commands, records/gallery, directional/state pairs and maintenance.
The former guide is preserved in `material-scale-casebook.md`, with only its
first heading renamed. The main guide links it and identifies dated notes as
historical evidence rather than the current task list.

Retained requirements include the owner-selected Tidal matching reference,
low-hull authority, component-level gloss checks, source-specific alpha cleanup,
native visual review, group-preview validation and separate owner acceptance.
Exact renderer limits remain owned by current code; asset histories remain in
the casebook and their existing handoffs. No art, runtime or executable tool changed.

`output/material-guide-consolidation.json` verifies original-text preservation
by hash and records line counts. Installed/project skill synchronization passes.
Next art tasks should read the concise guide, then only the relevant case notes.
Add a general rule for a new failure mode; keep per-asset findings in its record.
