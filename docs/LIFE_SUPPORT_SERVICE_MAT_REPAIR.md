# Life Support service mat repair

## Station-scale follow-up

`output/life-current-visual-v5` completes the current-controller tour across four
rotations with returns, child exit 0 and no ERROR/SCRIPT ERROR lines. Normal
52-percent composition captures include actual Battery neighbours and recovered
Bill; q0 and q1 were visually inspected at 1600x900. The helper's cell_pixels is
logical canvas sizing, not the final screenshot cell width after viewport stretch.

Visual finding: the service bench and supported tools are readable, and the
circulation stays clear. Full furnishing acceptance remains pending: the orange
maintenance cart reads as an isolated Engineering accent beside the grey-green
Life Support machinery, while the long q1 service run draws attention without
equally legible endpoint hardware. Prefer clarifying those existing activity
relationships over adding more isolated accessories. These captures have no
advanced operation clock and do not establish functioning-light acceptance.

The service-cart pad now measures 92x42 instead of 100x42, retaining offset
(-6,-9) and its colors. Unlike a bench-only mat, this wider service zone sits
beside a pressure bottle; only eight units were removed from its width.
The separate filter-bench mat, furniture placement, sources and hull are unchanged.

`output/life-mat-contained-v4.log` exits 0 for all four mat-bound checks.
`output/life-mat-review-v4` contains four native captures; q3 was visually
compared with `output/catalog-mat-review-v1/life_support-q3.png` and now leaves
clearance to the east wall.

`output/life-mat-state-v4.log` exits 0 without engine errors: seven assemblies,
four rotations, host motion/offline/containment and input isolation pass.
Its fixed injected-clock pause captures do not establish owning-clock pause
gating, and this fixture is not actual NPC travel or fresh packaged evidence.

Native 512-square card mat-v4 was baked, visually inspected and LFS-checked.
Card consumer, station fallback, alternate-art list and export manifest now
select it. The alternate list previously referenced two legacy room images;
those files are retained but no longer selected by this list. Profile hash updated.

- Profile SHA256: `4C3FB3D258E09BCEA693E7DEE6803DC4155189F095DA9958539F34343E964B39`.
- Card SHA256: `ED56719B7D8F5E52FD66B33F7718DC147092A98E9E7EA5D2CAFA3E01A9AA8361`.

Fresh batch packaging remains pending. The existing pipeline's composition and
consumer-reconciliation rules apply; no new bible direction was introduced.
