# Windows icon handoff

Updated: September 9, 2026 · Project: BrineSpace · Task: custom executable icon

## Objective and acceptance
Use BRINE's face as the game icon, following the owner's correction from an initially generated core emblem.

## Accepted decisions and constraints
Reuse `character/brine-comms-v14/portrait.png` directly. No regeneration or retouching of her face. The generated emblem is not bound to the game and is retained only in ignored local output.

## Current state
`project.godot` sets the current portrait as its icon. The Windows Game preset enables resource customization and sets the same PNG as its executable icon, with BrineSpace product/description metadata. Godot generates the Windows icon sizes automatically. Other export presets are preserved.

New playable folder: `builds/BrineSpace-2026-09-09-face-icon/`, containing EXE, PCK, README, NOTICE, build_info and SHA256SUMS. Prior polished build remains intact. Build ID: `brinespace-501dcca927427024`.

## Verification
Final maintained export passed without error logs. Windows extracted embedded icons at 16, 32, 48, 64, 128 and 256 pixels; native 32/256 outputs reviewed and show the selected portrait. Actual release title launch for 120 frames and clean exit passed without runtime errors. Logs and extracted icons: `output/app-icon/`.

An initial export started before configuration changes were applied and was rejected by manifest validation; it is not the delivered export. Final export regenerated the manifest after the settings were fixed. Gameplay code is unchanged; prior gameplay acceptance is documented separately, not rerun or claimed as new evidence here.

## Next action
Use the face-icon build for sharing. Further icon cropping or styling can follow owner feedback; current source and executable both use her existing portrait.
