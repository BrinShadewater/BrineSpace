# Marsh ground transition integration

Updated: September 13, 2026. Project: Brine Space.

## Objective and acceptance
Continue the owner-requested animation/movement polish, with whole-body weight transfer and credible foot contact. This milestone integrates the reviewed east start/stop; it does not close the broader goal.

## Accepted decisions and constraints
Marsh is a blond male android with an anatomical-right temple implant and no helmet. Veld is a woman without glasses. Bill stays unchanged. Other directions require their own authored transitions.

## Current state
- `scripts/marsh_ground_motion.gd` and paired UID now sit between Marsh and the unchanged Bill controller. They preserve base collision/arrival behavior, serialize optional ground_start/ground_stop metadata, cancel incompatible poses, and arm starts when job selectors pre-set walk state.
- `scripts/marsh_npc.gd` uses that base and validates its snapshot metadata before the existing battery fields.
- `character/marsh-v2/supplemental/ground-east/` supplies two four-frame, 100-ms, nonlooping states at 256-square, pivot 128/224, standing density 148. The runtime catalog loads them normally. Stop scope is dry east, final segment, empty goal/stage; starts require dry east and empty stage.
- `tools/build_marsh_ground_transitions.py` packages selected source pixels and provenance; `tools/rebuild_marsh_art.py` retains the pack on rebuild. `tools/crew-art-source-contracts/marsh-supplemental.json` independently records exact timing, registration and PNG hashes. Original source contracts are unchanged.
- `tests/playtest_marsh_walk.gd` probes now exercise production behavior. Legacy trial aliases/flags remain for historical commands; production-motion explicitly uses the normal controller and catalog.
- Validator, complete-pack check, 810-row clip ledger, visual bible, current status and maintained/installed character pipeline reference updated.

## Verification
- Snapshot/start/stop unit checks pass (`output/crew-replacement-2026-09-12/marsh/*-production-final.log`): variable delta, exact arrival/timer, zero delta, blocking, reversal, action/death precedence and metadata consistency. Subsequent route-selection hooks are covered by native integration below, not a claim of all route cases.
- Final production controlled native log `ground-motion-production-final-controlled.log`: exit 0, 54 samples, encoded actor restores during start and stop. Contact/transition pages reviewed in `live-idle-transition-east-snapshot-01-production-01`.
- Final autonomous native log `ground-motion-production-final-autonomous.log`: exit 0, 8 east samples into north kneel, battery 92.47, no helmet; contact reviewed in `live-walk-review-east-padded-production-01`. This bounded approach does not prove every job-selection entry.
- Battery/recharge/save regression: `output/test-runs/20260913-002241-headless`, PASS, 116 route samples.
- Complete packs initially rejected the added states by original-only coverage. Explicit supplemental checks now pass 19,307 checks, zero failures: `output/test-runs/20260913-002444-headless`.
- Art validator: 142 body states, 684 references, zero errors/border touches; 211 original source frames and original manifest unchanged. Clearance finalizer reports 142 states.

## Next action
Review higher-cadence and short-route entry/exit behavior; active-transition Save/Continue now passes the disk/staged-restore checks below. Extend authored transitions to the remaining directions and review goal-specific arrivals. Continue Marsh north/south idle and action continuity, Veld carry and Branforth older kneel/carry. No export or owner acceptance claimed. No active job from this milestone.

Short-route timing follow-up: destinations 1/5/10 world units into the start exposed update-size-dependent arrival idle (short-start-before.log). The controller now inverts its distance curve to consume elapsed time after actual arrival. Start, stop and snapshot suites pass in short-start-after-*.log. A 5-unit native route passes 32 samples (short-start-native-5.log); boundary review still shows the truncated start returning abruptly to idle. Timing is repaired, but a dedicated short repositioning motion or compatible settling treatment remains required.

Full Save/Continue follow-up: tests/test_marsh_battery.gd now writes and reads the actual run-save file during both walk-start and walk-stop, invokes restore_staged (the Continue path), reacquires the replacement controller, and compares foot, timer, action clock, state and selected texture bytes. Pause freezes each restored transition and unpause advances it. The original battery fixture is restored before its existing tests continue. PASS in output/test-runs/20260913-003052-headless, 116 recharge-route samples. This establishes save-path/selected-texture continuity in a headless integration fixture, not a native visual review of the Continue overlay or every transition phase. No runtime changes were needed for this check.

Short-step source study submitted from the reviewed east standing identity. Exact prompt and durable provider job: character/marsh-motion-polish-v1/sources/short-step-east-video-01.{prompt.txt,job.json}. Provider status was confirmed in_progress from the exact-prompt job listing; active CLI wait session 7711 remains live. Log: output/crew-replacement-2026-09-12/marsh/short-step-east-generation-01.log. Review criteria: character/marsh-motion-polish-v1/short-step-east-review-plan.json. Resume this job; do not resubmit because output is buffered or observation times out. Source is not yet downloaded, reviewed or selected.

Short-step source 01 completed (1,131,292 bytes, 97 frames, 24fps, 1248x1664), downloaded and overview reviewed. Rejected: three-quarter frontal pose from frame zero and insufficient step/catch-up movement. Job JSON preserves rejection. Schema confirms dedicated start_image support; source 02 now uses --start-image with the same canonical east reference and clearer foot sequencing. Active wait session 39776, log short-step-east-generation-02.log, exact-prompt durable job short-step-east-video-02.job.json. Resume existing request; no runtime selection.

Revision 02 completed and downloaded (996,176 bytes; 97 frames, 24fps, 1248x1664). Overview preserves side facing and shows a complete small step/catch-up. Extracted seven unselected poses at source frames 18/26/34/42/50/58/66 using scale 147/1321 and position 66/54, provisional 120-ms holds: short-step-east-candidate-recipe.json, review/short-step-east-candidate-02/contact.png inspected. Source forward displacement remains in the fixed registration; root-motion tracking, selected-idle endpoint comparison and native tiny-route integration are next. No active generation session.

Higher-cadence production check: native 30fps controlled east route passes 164 samples (ground-motion-native-30fps.log). State boundary page inspected. review_crew_live_walk.py now rounds cumulative GIF timestamps to 10-ms boundaries; encoded duration verified 5470ms against trace 5466.667ms. Previous per-frame rounding would shorten 30fps playback. Fixture supports motion-fps=10/30/60 with 1.5 seconds of idle preserved at either end. This run establishes 30fps coverage, not 60fps acceptance.

Short-step root-motion preparation: fixed-registration endpoint/foot closeups reviewed. Final heel displacement is approximately 9 dense pixels (~3.97 world units), uncertainty about 2 pixels. Provisional root track [0,0,2,6,9,9,9] is recorded in short-step-east-root-recipe.json; torso motion beyond that track remains in the poses. Extractor now optionally subtracts explicit integer root displacement, leaving scale and vertical registration unchanged. All seven inverse translations reproduce original fixed-registration PNG pixels exactly (root-reconstruction-check.json). Selected-idle/start/end comparison reviewed in review/short-step-east-root-candidate-02/idle-endpoints.png. Candidate remains unselected; exact-distance native contact, timing and interruption/save behavior are next. This is a fitted track, not independent foot-lock evidence.

Short-step native trial: tests/playtest_marsh_walk.gd now has opt-in ShortStepTrial and short-step-trial flag, loading the unselected seven-frame manifest. It follows the provisional root curve over 0.84 seconds on exactly 3.9697297297297296 world units, using underlying collision/arrival and retaining a settling pose after arrival. Native 30fps run passed 104 samples (short-step-native-02.log); state-transition page and short-step-contact-closeup.png inspected in live-idle-transition-east-short-step-02-fps-30-route-3.9697297297297296. Trailing-foot catch-up completes before idle, removing the prior truncated-start boundary for this route. Scope remains one exact-distance fixture: variable-delta arrival accounting, interruption/death, save/restore and route-length policy are not yet validated for ShortStepTrial. No production short-step promotion or ledger acceptance.

Short-step timing/cancellation probes: short-step-unit-before.log reproduced arrival timers 5.4833/5.5/5.6/5.7 instead of 5.48 seconds after one second of simulation, plus stale poses after route replacement/cancellation. ShortStepTrial now accounts for time after its .48-second destination arrival and invalidates pose overrides against the current path or settled foot. short-step-unit-after.log passes 1/60/.1/.3/.7 update partitions, one exact arrival, zero delta, blocking, route changes, death and new actions. These fixes are fixture-only; source artwork and production behavior are unchanged. Short-step snapshot and route-length integration remain open.

Short-step checkpoint follow-up: trial now serializes elapsed/target metadata, validates canonical Marsh state plus finite clock/target, rejects overlapping start/stop data and restores through the actor graph path. Direct unit checks passed, but first native restore failed: the normal route-selection hook could arm a ground start during an active short step. ShortStepTrial now suppresses that hook while active, with an explicit regression probe. Final unit and native encoded actor round trips pass in short-step-snapshot-after-{unit,native}.log; native captured 104 frames and restored both travelling and settling phases. This is actor snapshot evidence, not full disk Save/Continue for short-step integration. Production routing/selection and full save verification remain open.

Short-step route-range trial: supports 3.5-4.5 world units on a horizontal dry east final segment with empty goal/stage. Compared with nominal 3.9697, endpoint variation is at most ~1.2 dense pixels, within the provisional ~2px landmark uncertainty; this is not a universal deformation allowance. Step length is saved/validated and scales the recorded travel curve, not the artwork. The route hook now arms the short step even when a selector has already set walk state. Unit checks verify both endpoints, timing, saved length bounds and rejection of 1/3.49/4.51/10-unit routes. Native 30fps runs with travelling/settling restores pass 104 samples at each boundary (short-step-range-3.5.log and short-step-range-4.5.log). short-step-range-contact.png reviewed; foot catch-up and idle joins remain compatible at both boundaries. Production promotion and full disk Save/Continue are next; other tiny distances remain unresolved.

Production short-step milestone supersedes earlier unselected/trial-only status. Current implementation, scope and checks are reconciled in HANDOFF_MARSH_SHORT_STEP_2026-09-13.md.
