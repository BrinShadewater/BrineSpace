# Bill alternating west walk integration

Updated September21,2026. Project: BrineSpace. Broad goal remains unfinished.

## Objective and decisions

Replace the repeated west support-leg poses behind the owner's disjointed/shuffling
feet report with six connected alternating whole-body poses. Preserve the existing
east recipe, other actions, timing/stride, owner rooms and library marks. No
Higgsfield, commit, publication or new release export.

## Current state

`tools/rebuild_bill_art.py` now selects `build_bill_alternating_west_walk.py` for
walk-west after the original base rebuild. The helper replays frozen whole-body
registration, masked surface detail, hard-alpha finalization and48x56 helmet fits.
`build_bill_connected_walk.py` and the normal `repair_bill_walk.py` review command
now export the selected east and west recipes. The surface tests distinguish
east's repeated registered source head from west's independently authored poses.

Exactly12bare/helmet west-walk PNGs changed. All4448other inventoried runtime art,
metadata and import files are unchanged. Twelve decoded frames exactly match the
finalized native-reviewed candidates. No other state/direction, controller,
clearance, pivot, frame count, timing or stride change. Source recipe metadata's
staged status records its freeze point; this integration supersedes that status.

Sources/prompts: `character/major-bill-v3/sources/west-walk-alternation-2026-09-21/`.
Prior [source/native study](BILL_SIX_POSE_WEST_REVIEW_2026-09-21.md) records the
eleven built-in generations, rejected outputs and surface/alpha processing.
This integration required no additional generation.

## Verification

Evidence: `output/bill-west-walk-integration-2026-09-21/`.

- Complete rebuild passed; inventory delta and12reviewed pixel comparisons passed.
- Four surface/review-command tests passed, including decoded west hashes,
  binary alpha and exact lower-body preservation beneath the helmet.
- Full library validator:2214frames,780original frames and113source manifests
  preserved, no errors or border touches.
- Actual production loader:11773checks,0failures.
- Installed paid station fixture: exit0,480samples,82unpaused west walks; building
  costs/failures enabled. Actual getter verified on each westward walking frame,
  with no candidate manifest or texture-table overrides. One west idle sample,
  subsequent north/east/south travel. This does not establish a long idle/work
  transition or a full expedition. It uses controlled room setup and manual30Hz
  simulation, following the actor with the camera.
- Native station context inspected; part of the route is dim because of actual
  station lighting. Separate native production-player capture provides27well-lit
  frames at source and384-unit room scales. `installed-west-walk.gif` is a crop
  of the installed source-scale render,27frames/900ms, not altered station lighting.
- Earlier bare/helmet candidate visual review still applies to the12identical
  decoded installed outputs. Owner gait judgment and anatomical world-space foot
  locking remain unproven.

## Next action

Review owner feedback on the west motion and address east separately, retaining
connected limbs and the same appearance across actions. Continue room/polish work
without editing protected layouts. Current1fe9515ad1c5f4c5Windows/Mac packages
predate this west-walk integration; no packaged acceptance is claimed for it.
