# Room finishing pass

Owner requested the remaining listed polish and verification. The current live
catalog has 35 identities: 33 room views and two procedural corridor types.
Runtime coverage is not final visual acceptance.

## Existing-room queue

The rectangular-mat catalog sweep now passes all 128 placements after Anomaly
and Maintenance repairs. Both cards are updated; Maintenance's native state and
1092 route samples pass. Anomaly's q3 lamp contact is now repaired, with its
unchanged overlap assertion passing and a fresh four-rotation bounds audit clean.
Fresh batch package v2 now passes the three-crew controlled tour: 48 arrivals,
135 reciprocal transitions and 10,434 collision/speed samples after the passage
fix. See `MAT_REPAIR_PACKAGE_FOLLOWUP.md`; multi-resolution state/visual review
and broader traffic coverage remain separate.
See `FINAL_CATALOG_MAT_REPAIRS.md`; catalog bounds success is not full art approval.

Crew Hab's chair/desk mats now remain inside all four hull orientations, with
their centers preserved. Native eight-assembly state checks pass. Card mat-v4
is selected including the legacy variant list; fresh batch packaging remains
pending. See `HAB_MAT_CONTAINMENT_REPAIR.md`.

Life Support's service pad now clears all four rotated hulls. Native seven-host
state checks pass, and card mat-v4 replaces stale primary/alternate mappings.
Fresh batch packaging remains pending. See `LIFE_SUPPORT_SERVICE_MAT_REPAIR.md`.

Reactor's service mat is now host-fitted and contained in all four rotations.
Card mat-v2 is selected in primary, variant and manifest consumers. Its 48 host
operation comparisons and four production-clock pause/resume pairs pass;
fresh batch packaging remains pending. See `REACTOR_SERVICE_MAT_REPAIR.md`.

Gravity Loom's bench mat now fits all four rotations, with card mat-v3 selected
and manifest/profile hashes reconciled. Native state checks and 1440 perimeter
standing samples pass. Fresh packaging remains pending. See
`LOOM_SERVICE_MAT_REPAIR.md`; these samples do not establish actual NPC traversal.

Archive now passes the same fresh package at 1280x720, 1600x900 and 2560x1440:
159 dimension-checked full frames, with input isolation at every size. See
`ARCHIVE_CLEARANCE_EXPORT_REVIEW.md`. A new live catalog mat audit
(`output/catalog-mat-after-archive-20260906-v1.log`) still flags 11 placements
across Reactor, Gravity Loom, Life Support, Crew Hab, Anomaly Lab and Maintenance.
Its exit 1 is expected outstanding containment evidence, not a passing catalog.

Archive clearance now passes a fresh Windows package: three-crew mixed station
tour (45 arrivals, 124 transitions, 9497 movement samples) and its individual
four-rotation state fixture at 1600x900 (53 dimension-checked full frames).
Packaged q3 room crop inspected. Wider resolution and work-surface travel
coverage remain separate. See `ARCHIVE_CLEARANCE_EXPORT_REVIEW.md`.

Data Archive's production-controller route now passes four rotated five-room
layouts: 24 arrivals including returns, 32 doorway transitions and 2613 movement
samples. Single recovered Bill only; not multi-crew traffic or visits to every
work surface. Fresh packaged verification remains open. Evidence and visit
capture limits: `ARCHIVE_SERVICE_MAT_REPAIR.md`.

Data Archive's repaired cart/terminal/lamp group now passes 36 static native
depth poses across four rotations; all 24 ordinary front/rear probes are
standable. Reversed ordering changes pixels in every comparison. This does not
establish continuous NPC arrival or multi-crew traffic. Fresh package evidence
remains pending. See `ARCHIVE_SERVICE_MAT_REPAIR.md`.

Data Archive's three registered-cutout contacts are repaired with local offsets;
the unchanged q1 lamp/cart pair has overlapping empty bounds only. Native room
state checks pass and a forced-contact negative control fails. New card
clearance-v3 is selected. Fresh package and crew-access review remain pending;
see `ARCHIVE_SERVICE_MAT_REPAIR.md` for evidence and earlier failures.

Data Archive's service-cart mat is contained in all four rotations and card
mat-v2 is selected. The broader native fixture reports four assembly-overlap
assertions, so whole-room acceptance remains open. Review terminal/cart at q0
and cart/lamp in the other rotations before changing placement or tests. See
`ARCHIVE_SERVICE_MAT_REPAIR.md`. All walls retain the owner's current low-height
contract; no raised-wall candidate is accepted by this repair.

Hydroponics harvest mat repaired and card mat-v2 selected in every current
consumer. Both failed rotations are visually contained; native four-rotation
state checks pass. The catalog now has 13 flagged mat placements across seven
other rooms. Fresh batch packaging remains pending. See
`HYDRO_HARVEST_MAT_REPAIR.md`.

Catalog mat audit finds 15 out-of-interior placements across eight rooms, from
128 checks over all live dressing helpers. Fifteen native diagnostic images are
available; Hydroponics q1 visibly crosses the west hull and is the next priority.
No bulk clamping applied. See `ROOM_MAT_CONTAINMENT_AUDIT.md` for exact scope and
the remaining flagged rooms.

Medical Bay bed/mat repairs now pass fresh packaged checks at 1280, 1600 and
2560 widths (207 actual-size full frames) and the three-crew mixed tour (45
arrivals, 124 transitions, 9476 Bill samples). The packaged q3 connection was
visually inspected. See `MED_FOOT_EXPORT_REVIEW.md`; broader crew overlap and
whole-catalog aesthetic acceptance remain separate.

Medical Bay bed-edge cleanup now integrated with card foot-v3, retaining the
contained preparation mat. Sixteen comparison renders match the reviewed study
exactly; retained-base checks, negative control and native four-rotation room
state checks pass. Fresh package verification remains pending. See
`MED_BED_FOOT_EDGE_STUDY.md` for the revision and historical rejected candidates.

Medical Bay preparation mat repaired in production: 82-unit width follows its
72-unit bench, stays inside every rotated hull and matches new card mat-v2.
The new containment gate fails the old q3 overflow; native four-rotation state
checks pass. Bed-edge candidate remains unselected; fresh package evidence is
pending. See `MED_PREPARATION_MAT_REPAIR.md`.

Medical Bay full-room follow-up: bed candidate passes 1500 retained-base samples
and rejects the broken-footboard negative. Rotated review exposes an existing q3
preparation-area mat outside the east hull in both baseline and candidate. The
review canvas now includes exterior margin at close-up scale. Fix that overflow
before whole-room acceptance; see `MED_BED_FOOT_EDGE_STUDY.md`.

Medical Bay bed-foot cutout study: source-floor wedges below the side rails have
an unselected contour candidate. V1's point-order mistake is rejected; V2 retains
the inspected footboards, with eight unrelated renders byte-identical. Hardware
retention and rotated full-room review are still required before integration.
See `MED_BED_FOOT_EDGE_STUDY.md`; production art remains unchanged.

Reactor exported state follow-up passes at 1600x900: motion, production pause,
economy suspension and lighting restoration, with 48 Reactor frame dimensions
checked. A same-PCK three-crew tour passes 45 room-instance visits, 124 transitions
and 9476 Bill samples after correcting an omitted-manifest invocation. See
`REACTOR_STATE_EXPORT_REVIEW.md` for package identity and retained failure scope.

Reactor economy lighting now passes native four-rotation suspension/resume:
six-unit generation stops, the 0.65-second light fade freezes midway on pause,
and restart restores original sampled brightness. The stuck-on negative fails
eight checks. The earlier lit offline image omitted the offline reason, rather
than exposing a production shutdown defect. See `REACTOR_LIGHTING_REVIEW.md`;
fresh exported execution remains separate.

Reactor native host motion/pause follow-up passes: 48 prop comparisons, four
explicit production-clock pause/resume checks, and an eight-failure frozen-chamber
negative control. This resolves the native pause gap, not economy-driven lighting
or fresh package verification. See `REACTOR_EFFECTS_REVIEW.md`.

Reactor prop edges: twelve offline dark/light diagnostic renders reviewed; no
new cutout repair selected. A source close-up confirms the cooler's lower backing
is a beveled mounting plate and must remain. Independent Reactor-host animation
and pause checks remain open. See `REACTOR_PROP_EDGE_REVIEW.md` for revisions,
observations and the diagnostic's dependency-provenance limits.

Crew Hab package follow-up: `hab-progress-export-v1` and a bounded replay of the
earlier v4 PCK both pass all four rotations, 20 arrivals and 1938 movement samples.
The prior q3 timeout did not reproduce; its cause remains unproven. Opt-in phase
logging now distinguishes navigation, movement and capture waits. See
`CREW_HAB_SETBACK_STUDY.md` for exact package hashes and retained failure scope.

Newest mixed-package checkpoint: `hab-setback-package-v4` passes 31 visits,
88 transitions and 6729 movement samples with three recovered architects.
Its 21 focused captures and loading gates pass. The separate q3-specific
exported Crew Hab route result remains pending; see `CREW_HAB_SETBACK_STUDY.md`.

Crew Hab rear-chair setback is integrated at q3 with its lamp and berth adjustment.
Bounds, sampled access, static depth and four-rotation production movement pass;
the reviewed pose no longer projects above the hull into the exterior. Broader
actor animation and fresh packaging remain pending. See `CREW_HAB_SETBACK_STUDY.md`.

Four-prop depth subset: Maintenance repair table, Crew Hab chair, Holo calibration
cart and Medical Center trolley pass native sorted-versus-reversed comparisons
in all four rotations (32 standable front/rear poses plus 16 forced probes).
Selected q0 full-room views were visually inspected. This does not certify all
room props or continuous crossings; see `THREE_CREW_ROOM_REVIEW.md`.

Latest three-crew Windows checkpoint: `three-crew-review-package-v1` passes 31
arrivals, 88 transitions and 6683 Bill movement samples, with 21 focused room
captures. Anomaly, Holo and Med Office frames were inspected; this is not complete
overlap approval. See `THREE_CREW_ROOM_REVIEW.md` for exact scope and next review.

Three-crew native follow-up: `three-recovered-tour-v4` passes 31 arrivals,
88 transitions and 6686 Bill movement samples after production thaw, with a
fixture-added Crew Hab supplying real berths. The no-power control rejects
recovery. The subsequent package is recorded above; neither is fully depth-approved. See
`THREE_CREW_ROOM_REVIEW.md` for explicit setup and assertion scope.

Latest checkpoint: `anomaly-recovered-package-v1` passes 30 arrivals, 86
transitions and 6498 collision/speed samples after the fixture completes Bill's
production core recovery. Only Bill is awake: this is single-crew traversal,
not three-crew traffic. Capture metadata now records active/present crew.
See `ANOMALY_RECEIVER_TRIM_REPAIR.md`; full visual approval remains open.

Earlier successful mixed-tour checkpoint: `xeno-status-package-v1` passes 30
arrivals, 84 transitions and 6352 movement samples with the updated Xeno status
strip. Individual Xeno checks pass all three actual sizes (159 full frames).
See `XENO_WORKBENCH_STATUS_REPAIR.md`; this does not approve every room's art.

Earlier `battery-join-package-v1` passes all 30
scheduled arrivals, 84 transitions and 6418 movement samples after the targeted
traffic graph-entry repair. Source/card/component/profile loading gates pass.
See `BATTERY_TRAFFIC_JOIN_REPAIR.md`; autonomous traffic remains separate.
That historical package predates the Xeno workbench status-strip repair.

Earlier `holo-status-package-v1` passes the 30-room
controlled tour (84 transitions, 6359 movement samples) and loading gates, including
the updated Holo status lenses. Its individual Holo checks pass all three sizes
(183 full frames). See `HOLO_STATUS_LENS_REPAIR.md`. V38 previously passed the tour
(84 transitions, 6473 movement ticks) and loading gates with the V5 drone layouts.
This is a frozen package, not proof of all subsequent checkout changes or all 35
current room identities. V37 separately verified Hull Integrity's formerly missing
profile as present, hash-matched and parsed in an isolated package probe.
See `ROOM_DRESSING_HOST_AUDIT.md`; historical failed runs below remain evidence,
not the current package status. Visual polish and autonomous traffic remain separate.

The intervening `biodome-canopy-package-v1` passes loading gates but fails its mixed tour
at Battery Array after 17 completed legs in a waiting-for-passage state. Its
individual Biodome tests pass all three actual sizes (183 full frames). Preserve
that split result; see `BIODOME_CANOPY_REPAIR.md` and the traffic diagnostic.
The newer passing tour does not retroactively change that failed package.

- Furnishing-host inheritance: Bio Lab no longer retains Biodome-only references.
  Its V33 individual exported state tests pass at all three sizes (207 frames).
  The native host-resolution export gate resolves 33 views, 32 profiles and 492
  references in the newer Biodome package preflight, plus two procedural exclusions. Both
  missing-host and unmapped-room negative controls fail as intended. Earlier
  26-view gate versions ran in V34/V37/V38. This is native pre-export host checking,
  not a whole-package visual pass. See `ROOM_DRESSING_HOST_AUDIT.md`.

- Drone bays: shared fleet atlas now packages correctly. Dock diagnostics repair
  passes native 1600 four-quarter motion/offline/pause and route tests; refreshed
  V5 activity-area layouts and matching cards are integrated. V38 passes both
  exported room fixtures at all three sizes (366 full-frame dimension checks).
  Current-layout static depth checks pass 48 poses; native single-crew cradle
  circuits pass eight rotations/rooms and 1128 swept ticks. Bill's rear-path head
  projection above the low north hull remains visually unresolved. See
  `DRONE_DOCK_STATUS.md` for precise scope and sampled-motion review.

- Anomaly: capacitor glass/indicator banks repaired in v8; ten platform strips
  retain recessed lenses in v9. Native checks and individual v18 exported checks
  pass at all three actual sizes (159 full frames). The mixed tour failed at Holo;
  tiny supplemental masks and owner review remain separate.
  A newer receiver fascia repair selects trim-v10 and passes focused material
  checks. The revised native fixture separates unoccluded machine measurement
  from crew-bearing review; zero assertions, with an eight-failure missing-motion
  negative control. Fresh trim packaging loads assets but fails mixed-tour NPC
  initialization. Exported individual state checks are recorded separately and
  do not establish crew depth: current architect-presence gating leaves the
  fixture crew inactive. See `ANOMALY_RECEIVER_TRIM_REPAIR.md`.
  The recovered-package follow-up now passes the station tour with Bill awake;
  multi-crew depth/traffic review remains distinct from this single-crew result.
- Xeno: five vessel lamp interiors repaired in card v4, preserving surrounding
  housings. Native checks and individual v20 exported state checks pass at all
  three actual sizes (159 full frames). Tiny supplemental masks and owner review
  remain pending. A newer workbench near-white status strip now turns off with
  operation; status-v5 card and native four-rotation off/on/economy checks pass,
  and fresh packaging now passes all three sizes (159 full frames) plus the
  mixed tour. See `XENO_WORKBENCH_STATUS_REPAIR.md`.
  Earlier traffic result:
  V20's ordinary station tour stalls in Crew Lounge crew traffic;
  this separate traversal failure is retained, not counted as an art-state pass.
- Biodome: right processor pipe opening retained; upper-left fern fringe repaired
  in card v4 with native checks and v21 individual exported checks at all three
  actual sizes (183 full frames). V21's ordinary 30-room tour also passes.
  The newer tree-canopy notch and two enclosed-gap repairs select
  `biodome-card-canopy-v6.png`: four-rotation state/route checks pass, with eight
  non-tree diagnostic images unchanged. See `BIODOME_CANOPY_REPAIR.md`.
  Individual packaged tests pass all three sizes (183 frames); its mixed tour
  fails in Battery Array crew traffic, not in the Biodome fixture.
  Remaining foliage/pipe margins and owner review are separate; this pass does
  not establish a fix for earlier intermittent crew traffic.
- Holographic Core: calibrator contour retained; terminal glass restored in card
  v7. A newer four-status-lens pass uses card `status-v8`: all 189 bright source
  cores are covered, negative control fails, and native four-rotation actual
  economy/operation/pause checks pass. Fresh Windows packaging and all three
  individual resolutions now pass (183 full frames); see
  `HOLO_STATUS_LENS_REPAIR.md`. Earlier evidence below predates this repair.
  Terminal glass was previously verified in card
  v7 with native checks and v22 individual exported checks at all three actual
  sizes (183 full frames). Remaining cyan material/emission separation and fine
  edges are pending. V22's station tour fails at Research Lab with `route
  obstructed`. A two-room probe now reproduces a long-segment clearance false
  negative at the Battery Array doorway corner without any peer. The exact
  registered-blocker sweep now rejects it and the native replan reaches its target.
  Furniture is unchanged. V24 reaches all 30 packaged tour arrivals, but remains
  failed evidence: one collision assertion and a Life Support dressing JSON
  initialization error. V26 resolves the missing Life Support packaged profile
  after a file-scan refresh (isolated presence/hash/JSON and clean loading verified),
  but still fails one collision assertion despite reaching all 30 destinations.
  Native diagnostics also expose a waypoint-chord test
  false positive. The test-only traveled-leg observer now passes the same native
  trajectory and V28's complete 30-arrival packaged movement checks. V28 remains
  rejected overall for new drone-atlas loading errors; package art/dependency
  acceptance is separate from movement. V30 fixes the raw-export root omission
  and passes the complete packaged loading and 30-arrival controlled-tour check
  without engine errors. New drone props still need visual/state/card review. See
  `ROOM_TOUR_TRAFFIC_DIAGNOSTIC.md` for exact scope and the remaining checks.
  The exact old waypoint was not retained. V18's
  controlled export tour missed physical arrival at Holo `(25,21)`; investigate
  separately from source/card loading, which passed. Failure evidence is retained.
  A native occupied-target diagnostic reproduces traffic cancellation; the
  fixture-only bounded retry now reaches all 30 rooms. See
  `ROOM_TOUR_TRAFFIC_DIAGNOSTIC.md`; v18 itself remains failed evidence.
  V19's ordinary standalone tour now passes all 30 rooms. Its occupied-target
  attempt instead stalls in crew traffic near Crew Lounge before reaching Holo;
  broad traffic recovery remains unverified and is separate from room-art polish.
- Reactor: cooler loop repaired in v3; steel/orange hull now integrated in card
  v4 with native checks and v17 standalone checks at all three actual sizes
  (402 full-frame dimension checks). Fine margins and current-NPC coverage remain
  pending; see `REACTOR_STEEL_EXPORT_V17.md` for precise scope.
- Med Bay: bedside-monitor wedges and console rear corner repaired in card v5;
  native 1600 four-rotation checks pass. Ambiguous chair-foot shadows/fine margins
  remain untouched. V16 standalone fixture checks pass at all three sizes.
- Full set: actual-dimension viewport checks, packaged rendering and owner
  visual review. Requested-width labels are not actual-size evidence.

Medical Bay v5 and Reactor v3 now pass the v13 standalone mixed-station loading
and controlled-traversal check at actual 1600x900. See
`ROOM_REPAIRS_STANDALONE_V13.md`; individual packaged state/three-size coverage
remains distinct from this completed package check.

Individual fixture bridge is now implemented and passes six consistency tests;
V14/V15 builds were rejected for seabed asset-loading errors during concurrent
asset changes. Those assets are now present and v16 passes all six individual
Medical Bay/Reactor runs with 609 actual-size frame checks. See
`FOUNDATION_ROOM_EXPORT_FOLLOWUP.md` for exact state/visual scope and retained
failed evidence; these are not autonomous-NPC or complete economy-state tests.

Completed subset: Biodome v3, Holographic Core v6, Xeno v3 and Anomaly v5 pass
standalone v11 at actual 1280x720, 1600x900 and 2560x1440, with 684 full-frame
dimension checks and input-isolation exercises. See batch-two
`EXPORT_VERIFICATION.md` for precise state/visual scope. This does not close
the remaining art queue or establish full-set resolution coverage.

## Underwater additions

Diving Operations, Aquaculture and Submersible Hangar are additions, not missing
replacement art. The owner has been asked whether to implement playable rooms
with unlocks/synergies or prepare art/designs first. Continue existing-room polish
while awaiting this scope choice. Do not invent large-footprint mechanics,
vehicle missions, occupants or resource systems merely to fit illustrations.

Retain the bible's 72-unit doors and south-facing machinery. Large-room footprints
must be checked against actual engine support before briefs or generation.
