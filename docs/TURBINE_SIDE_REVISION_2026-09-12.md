# Turbine side readability candidate

## Objective and constraints
Address owner dislike of side views. Turbine stays on arrow wall. Exact objection remains unconfirmed; rotor visibility is the working diagnosis.

## Changed state
Authored east-readable candidate with broader left-facing intake, visible hub/blades and reduced opaque casing. Original source/prompt preserved under assets/turbine-directional-v2. Read-only vector registration lives with study, outside live library. Isolated native fixture replaces only east power_machine art; production registration/code unchanged.

## Verification
Native candidate reviewed at original machine rectangle. q0/q2/q3 RGB-identical to current fixture; q1 delta confined to machinery. Rotor reads clearly. Containing new silhouette at original aspect makes it slightly shorter. Initial fixture used wrong hyphenated ID and changed nothing; candidate-corrected is the valid evidence.

## Remaining
Owner visual direction needed to select this candidate. If selected, create matching west companion and verify updated operating anchors before installation. Do not report this study as installed or all turbine work complete. Full catalog remains active; no export.


## Elevated-camera study rejected

assets/turbine-directional-v2/turbine-east-overhead-study.png was generated from the earlier readable east candidate and reviewed in output/turbine-overhead-2026-09-12/native/current_turbine-q1.png. It retains inward intake/right backing but adds a large top hatch, still resembles vertically stacked equipment and shrinks the occupied wall length under the unchanged prop aspect. Native intake readability is worse. Kept as rejected study; no default, card, effect or production registration changed. Only q1 differs in the isolated candidate comparison, other three RGB-identical. Exact prompt, source hash, geometry and findings are in east-overhead-review.json.

Next: specify a coherent floor-mounted composition and wall footprint, not just a camera angle. Owner preference remains open; this study does not establish aesthetic acceptance.


### Turbine camera reconstruction study

A subject-only reference can still dominate the camera despite explicit overhead/floor-mounted instructions. September 12 east-low-study added a skid and readable inward rotor but retained much of the reference elevation and glossy generator edges. Keep it as a study, not a selected improvement. Next camera repair should supply an actual camera/layout reference or reconstruct without the misleading source; stronger angle adjectives alone have repeated the same geometry. Evidence: assets/turbine-directional-v3/east-low-review.json.


### Fresh turbine reconstruction and intake repair

Removing the old elevation reference produced a clearer overhead floor skid (east-overhead-fresh-study.png). It also moved the exposed rotor upward, so the next edit explicitly closed the top aperture and placed blades inside the inward side intake. Review both camera and functional opening independently: satisfying one can break the other. The repair still needs background/alpha and native material checks; it is not installed. Evidence: assets/turbine-directional-v3/east-intake-review.json.


### Clean overhead turbine native study

Imagegen replaced the baked checkerboard with white and subdued orange highlights. Read-only vector registration excludes the exterior without changing source pixels. The fresh source is wider than the old elevation, so preserving its world rectangle via contain registration shortens the occupied wall length. Native q1 now reads as an overhead skid with inward left intake on the east arrow wall, but scale/finish, west companion and functioning anchors remain before selection. Evidence: assets/turbine-directional-v3/east-clean-review.json and output/turbine-fresh-2026-09-12/native/current_turbine-q1.png. Native capture exits 0; this is candidate review, not owner acceptance.

## Later owner repair selection

The subsequent owner-wide art pass selects a new north overhead companion and muted revisions of the existing side/south geometry. This supersedes the pending north and orange items here without changing arrow or generation behavior. See [Current Turbine owner repair](CURRENT_TURBINE_OWNER_REPAIR_2026-09-12.md) and `assets/turbine-directional-v4/owner-repair.json`.


### Wider replacement artwork and saved positions

A candidate rect changed before render can still receive the saved default position during rendering. Turbine fresh fitted preview preserved 270 wall length and mirrored west correctly, but east crossed the hull because the old narrow-source position was reapplied. Validate final rendered geometry, and revise the candidate layout position with the source aspect rather than trusting pre-render rect values. Existing bounds-based rotor/lamp UVs also require remapping after a camera reconstruction. Evidence: output/turbine-fresh-2026-09-12/fitted and assets/turbine-directional-v3/east-clean-review.json. No live selection yet.

## Fitted candidate placement corrected

Added assets/turbine-directional-v3/fitted-layout.json as isolated candidate overrides. East x81.38678 and west x-184 preserve270length with102.61322width. Native capture exits0; runtime rectangles both contained in hull+-184, and east image reviewed with inward intake on arrow wall. Source still candidate: powered anchors and access/collision checks remain. Evidence output/turbine-fresh-2026-09-12/positioned. Production defaults and cards unchanged.

## Candidate source-local operating anchors

Added optional turbine_effects metadata passthrough in room_asset_library and source-coordinate effect drawing in power_room_view. Candidate fitted registration anchors lamp316,1247 and hub166,268 with26x92ellipse. Mirrored geometry maps both endpoints through source_uv. Native powered capture exits0; west image reviewed and four east/west lamp/hub off-to-on crop checks pass. Existing20side-variant regression passes20260912-135554-headless. Candidate source still uninstalled; temporal/retained and access checks remain. Evidence output/turbine-fresh-2026-09-12/effect-checks.json.


## Overhead turbine side assets installed

East clean source now supplies both side registrations, with mirror_horizontal metadata for west. Source-local effect points mirror through the same transform as art. Default east position updates with the wider footprint; all176layout routes and20side-variant checks pass20260912-135713-headless. Native installed captures at two clocks exit0; four effect crops change with time and west installed view reviewed. q0 card unchanged. Retained station pause/state and owner aesthetic acceptance remain. Evidence assets/turbine-directional-v3/east-clean-review.json.
