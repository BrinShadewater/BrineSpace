# Pressure Control and Listening Post directional completion

The accepted q0 U-shaped installations remain selected. Existing inward-facing west/east sources from assets/side-wall-inward-v2 are reused. Two new charcoal/brass and charcoal/teal south-wall banks complete q2. Source PNGs are unchanged; vector registrations exclude their white exterior. Generated image dimensions are read dynamically by the registration tool.

Runtime integration loads optional south registrations while retaining the horizontal footprint. Relocation uses the existing clear-space solver. The original q0 artwork and owner Layout Studio drafts are preserved.

Validation: full-wall review passes 15 rooms x 4 rotations x 2 operating states, with installation bounds, door lanes and relocated-art overlap assertions. Native architecture review captures all 40 furnished rooms in four rotations. Floor anchor audit passes 320 placements, zero missing hosts. Native q2 captures confirm restrained palette and unobstructed furniture.

Forty furnished-room cards were rendered through selected live room views, current floors, raised walls and foundations at 512 square. Framing was corrected after visual inspection to include both wall crowns and foundation feet. RoomCardArt selects the new cards; the three existing corridor cards remain selected. Card manifest records source hashes. This is a native render, not a raster recolor.

Evidence: output/rare-directional-v1, output/room-art-audit, output/rare-anchors.log, assets/rare-directional-v1/manifest.json and cards/manifest.json. The source manifest records generation briefs and references, not exact full tool prompts. Existing legacy image-load export warnings remain outside this change.

Selected-card inventory: 43 entries decode with no errors. Studio fixture now clears only its own stale recovery file before keyboard checks; this avoids a recovery modal intercepting R on repeated runs. Owner files are untouched. Installed and project skill snapshots match across all 14 checked files.
