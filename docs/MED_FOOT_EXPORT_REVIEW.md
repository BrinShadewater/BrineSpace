# Medical Bay bed-foot and mat export verification

Fresh package: `output/batch-two/med-foot-package-v1`.
PCK SHA-256:
`2C2F66A8C691F8334D2B2FC56B05B00B050C636E71D4C88C0AE65BF5722D6E69`.

## Completed checks

The helper completes import, floor-hook audit, resolved dressing-host audit,
Windows export and external-directory station runtime without engine/script
errors. Seven export-bridge consistency checks pass before and after the run.

The mixed station fixture passes 45 scheduled room-instance arrivals, 124
reciprocal door transitions and 9476 Bill collision/speed samples with three
active/present architects. Loading checks pass 29 selected sources, 58 raw PNGs,
44 component images and 26 composition profiles. Recovery uses fixture
preconditions; this is not a paid construction or autonomous destination test.

The same PCK passes individual Medical Bay checks in:

- `output/med-foot-export-1280-v1`: 69 verified full frames at 1280x720.
- `output/med-foot-export-1600-v1`: 69 verified full frames at 1600x900.
- `output/med-foot-export-2560-v1`: 69 verified full frames at 2560x1440.

Each run exits 0, logs no engine/script errors and passes injected input
isolation. The helper checks actual PNG headers against state sidecars: 207
full-frame checks total, not merely requested-width labels. Subject assertions
cover four rotations, assembly containment, per-host powered/offline effects
and socket infill. Legacy progress samples remain separate from the mixed
station's current-controller movement evidence. Raw-image warnings remain.

## Visual scope

Inspected `med-foot-export-1600-v1/med_bay/med-q3-connected.png`: the preparation
mat stays inside the east wall, the bed bases retain their revised outlines,
and the shared connection remains visible. This selected image does not establish
all-resolution aesthetic approval, every seam, or continuous crew overlap.

The selected card remains `med-bay-card-foot-v3.png`, with the contained 82x39
preparation mat and reviewed bed contour. Native pre-integration comparison,
retention and negative-control evidence is in `MED_BED_FOOT_EDGE_STUDY.md` and
`MED_PREPARATION_MAT_REPAIR.md`. This fresh package closes their pending export
follow-up for these repairs. Broader room finishing remains open.
