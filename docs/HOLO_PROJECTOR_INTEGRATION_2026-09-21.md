# Project handoff

Updated September21,2026 · BrineSpace · Holographic projector layer repair

## Objective and acceptance
Repair baked projector emission and its coarse enlarged appearance, preserving
owner rooms and pack source. Continue the broad animation/art/performance goal.
No Higgsfield, commit, push or publication. Owner visual acceptance remains open.

## Current state
Installed a room-specific unpowered projector housing with a separate clock-driven
star-field projection in rooms/full-wall-v1/holographic_core_view.gd. The housing is
static. Operating state controls the entire projection and its screen trace. Custom
library draw/animation classification keeps it live in the retained renderer; copied
source IDs are recognized. Restored the existing calibrator at its saved position.
Four complete Holo keys installed in defaults/save; all other keys guarded unchanged.
Refreshed assets/room-cards-v2/holographic_core.png (offline housing, as card tool uses
off state). Original bought atlas and owner registry marks remain untouched.

New assets/holo-projector-v1 contains reference crop, exact prompt, generated source,
normalized projector and provenance. One built-in image_gen edit, no Higgsfield.
Raw1536x1024 RGBA has alpha0..254. tools/rebuild_holo_projector.py removes only alpha
below8, crops [116,204,1421,830], nearest-fits housing256x123 and places it at y96 in
a256x240 transparent canvas. No repainting of source pixels in normalization.
PNG files resolve to LFS. Registration holo-projector-v1.json uses130world width;
upper transparent area reserves effect bounds. Explicit collision_boxes cover the
physical housing; footprint supplies shadow only. This is not a collision bypass.

## Verification
Four candidate/installed native views each pass640 walking samples. All candidate
views reviewed; installed full RGBA parity exact in all four. Four-view effects
fixture verifies changed pixels within projector/calibrator bounds, off stable and
held-clock stable. The first cylindrical lattice was refined into a restrained star
projection; operation-layered-r3.log is the final effects result. Earlier operation
image paths were overwritten by that final fixture; use logs to distinguish runs.

Extended existing test_removed_bank_restoration.gd (UID retained) verifies saved
calibrator coordinate, projector presence/custom path/live classification across
four rooms/four quarters/repeated setup:0failures. Native live-r3 fixture completes
52/75-percent captures and actually runs the station visual clock, then pauses via
_set_paused(true): clock and complete room RGBA remain exactly unchanged across
0.4seconds plus rendering settles. Native room crop, paused image and completed card
reviewed. Funded isolated fixture is not paid-balance acceptance or full expedition.

## Next action and limits
The separate bought cyb-146b chart still has baked luminous imagery without operating
metadata. Repair its offline/operating layers next. Room composition/density and owner
visual acceptance remain open; no rear-body actor occlusion claim. Current c09c
packages predate this integration and recent room changes. Batch release after
focused follow-ups. Continue Bill/other animation and measured performance work.
