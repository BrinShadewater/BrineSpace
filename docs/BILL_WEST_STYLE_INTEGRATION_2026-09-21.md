# Bill west work style integration

Updated: 2026-09-21. Broad project goal remains active.

## Objective and decisions

Match the west work body to Bill's canonical standing/walking identity without
reintroducing disconnected legs. Use independently authored connected whole poses,
one standing-derived scale, registered front-boot contact and normal helmet size.
Two built-in image_gen calls expanded the earlier identity pair into lowering and
tool-loop sources. No Higgsfield, paid API fallback or owner-room edits.

## Installed state

`tools/build_bill_west_style_actions.py` is now selected by the canonical rebuild.
The earlier west helper/source remains historical. New sources, exact prompts,
SHA hashes and registrations live in
character/major-bill-v3/sources/west-actions-style-2026-09-21/.
Draft status strings in source-study metadata record creation stage; this handoff
and CURRENT_STATUS supersede them for installation status.

Lowering uses six connected source poses at scale148/552, with whole-frame placement
registering the front-most boot band at x106 and sole bottom224. No limb warping.
Work keeps the settled body, head and feet, replacing only the recorded arm/tool
polygon on four interior poses. Work endpoints equal settled kneel; stand reverses
kneel. Equipment uses the standing48x56 west shell and per-pose fitted head anchors.

Exactly36bare/helmet kneel/repair/stand PNGs changed. Every other captured runtime
art/metadata file, including clearance, is byte-identical. All36 match the reviewed
candidate exactly. Existing release packages predate this integration.

## Verification and evidence

Evidence: output/bill-west-style-2026-09-21.

- Candidate lowering/work/helmet sheets inspected at source and game scale.
- Native-r2 captured198frames per equipment state with zero failures. Actual bare
  render selection was checked throughout the action sequence, not just the loader.
  Bare/equipped working captures inspected. Direction and gear are fixture-forced;
  this is render/action coverage, not autonomous facing/donning or full expedition.
- The first native capture was invalid for bare art: appending a player manifest
  left the grid's separate dry-frame table stale. Corrected fixture updates both
  tables and asserts the real getter returns candidate textures. Native-r2 supersedes
  native/; no runtime renderer change was needed.
- Canonical rebuild, two action-continuity tests, three-direction helmet-shell test,
  full2214-frame validation and11773installed-consumer checks pass.
- All780original source frames and113original manifests preserved; no border touches.
- Contact test verifies the visible front-most boot band, not complete anatomical
  foot locking. Work pixels outside the arm mask remain identical to settled kneel.

`west-actions.gif` is the corrected native capture at2x nearest crop enlargement:
99every-second samples from30Hz,60/70/70ms encoding totals6.6seconds. Its pixels
match the installed result; it is not an autonomous playtest.

## Remaining work

Owner motion acceptance, ordinary expedition transitions, broader cast/art review
and the remaining furnishing/performance/release goals remain open. Review north
and south body consistency against standing before selecting further repairs; do
not regenerate already accepted states merely to make a batch uniform.
