# Archive clearance export review

Fresh Windows validation package:
`output/batch-two/archive-clearance-package-20260906-v1`.

PCK SHA256:
`977124317A79B4DEE91FE04CF85E3A233D604969B6C642DCEF775221B44A413C`.

Import, floor-hook audit, resolved dressing-host audit, export and external-directory
runtime pass. Seven bridge consistency tests pass after fixture generation and
again after runtime. Archive renderer/card hashes remain those recorded in
`ARCHIVE_SERVICE_MAT_REPAIR.md`.

The packaged mixed station completes 45 scheduled arrivals, 124 reciprocal door
transitions and 9497 checked Bill movement samples with three active/present
architects. Recovery has fixture preconditions; this is not paid construction,
autonomous destination selection or complete traffic acceptance.

The same package passes the individual Archive fixture in
`output/archive-clearance-export-1600-20260906-v1`, from an external working
directory. Child exit 0, no engine errors, input isolation exercised and 53
full-frame PNG dimensions checked at 1600x900. Assertions cover all four
rotations, registered cutout separation, containment and economy-driven operating
states. Injected-clock repeats are not owning-clock pause proof.

The packaged `data_archive/room-q3.png` room crop was visually inspected: the
service cart, lamp and terminal remain separated and the mat is contained. This
crop is not one of the dimension-checked full frames and does not establish
all-resolution or whole-station visual approval.

The same package additionally passes at 1280x720 and 2560x1440 in
`output/archive-clearance-export-1280-20260906-v1` and
`output/archive-clearance-export-2560-20260906-v1`. Each exits 0 without engine
errors, exercises input isolation and verifies 53 full-frame PNG dimensions.
Across all three sizes this is 159 dimension-checked full frames. These extra
checks establish runtime/dimension coverage, not manual review of every frame.

This closes the three-size fresh-package follow-up for the Archive repair.
Full working-surface travel and broader catalog finishing remain open.
Existing raw-image warnings are not suppressed.
