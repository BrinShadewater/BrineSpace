# Battery Array north camera repair

The orange-brightness pass is already verified, but its own handoff leaves the
north camera open under the current top-down owner contract. East, south and west
remain selected and are outside this repair. Preserve four closed cell modules,
three closed distribution units, three gauges, compact amber indicators, current
split-wall geometry and gameplay behavior.

New raw source and exact prompt live in `assets/battery-owner-v3`. The candidate
uses a low overhead camera with rear pipes at the top and latches, handles, gauges
and switches facing down toward the room. It retains graphite steel and restrained
rust-brown frames from the accepted palette. `build_battery_owner_north.py` removes
the magenta key and separates the two complete banks without resizing them; source
hash, crop dimensions and exact inventory counts are in `north-review.json`.

This is a cleaned source checkpoint only. Remaining: register the two banks into
their existing north frames without changing collision, inspect native q0 and
source scale, verify other quarters remain unchanged, run state/retained checks,
refresh the card, and record any workflow lesson. No gameplay, character animation,
export or owner acceptance.

Integrated checkpoint: both north registrations now select the cleaned v3 sources
inside the existing two 136-unit split frames. The new source ratios reduce the
visible bank depth while the split-wall collision rectangles, section IDs and
placement algorithm remain unchanged. Native q0 at
`output/battery-owner-north-repair-2026-09-12/native` was reviewed: four cell lids,
three distribution lids, three gauges and inward bottom controls remain readable;
the centered north doorway and crew floor stay clear. Exact RGB comparison against
`output/battery-orange-repair-2026-09-12/native` proves q1, q2 and q3 are unchanged;
only q0 differs. All 176 layouts and 20 side registrations pass in
`20260912-214949-headless`.

The selected q0 card is refreshed from the native capture, and all 47 card
identities pass in `20260912-215133-headless`. The three-state fixture at
`output/battery-owner-north-repair-2026-09-12/states` has exact direct/retained
parity. Its two powered north-wall clocks are pixel-identical. Offline versus
powered differs by four pixels with a maximum one-channel delta of one across
separately instantiated GPU textures; `state-review.json` records that bounded
raster sampling tolerance. The larger whole-room differences are confined to the
preserved independent breaker effect.

The owner-reported north camera and orange issues are therefore satisfied in the
source workspace: q0 uses the low overhead inward-facing v3 companion, q1-q3 retain
the previously verified muted family, and inventory, placements, collision and
gameplay behavior are unchanged. This is agent visual verification, not owner
acceptance; no gameplay, character animation or export work was performed.
