# Medical Bay bed-foot edge study

Status: reviewed V2 contour now integrated. Earlier candidate evidence below is
retained chronologically; see the integration follow-up for current consumers.

`output/med-bay-current-edge-review-v1` contains twelve native offline prop
renders against light/dark backgrounds. The console pair and first bed's dark
image were inspected; this is not a completed twelve-image visual review.

The read-only `med-console-foot-detail-v1.png` source crop shows a genuine raised
console base and stool casters surrounded by baked shadow. Do not erase pale
base trim as if it were floor. Fine caster-edge ambiguity remains unmodified.

The first bed's dark capture exposes narrow pale wedges below its projecting
side rails. `output/med-bed-foot-detail-v1.png` inspects source rectangle
`[210,455,190,95]` at 3x nearest-neighbour zoom: the base sides step inward below
the rails, while the current contour bridges that inset diagonally with floor.

## Candidate and rejected attempt

`rooms/whole-room/med_bed_foot_edge_study.gd` subclasses Medical Bay without
changing its production dispatch. It inserts bed-source points `(384,494)` on
the descending right edge and `(229,494)` on the ascending left edge, with the
existing second-bed source offset. Footprints, sort lines and source pixels
remain unchanged.

- `output/med-bed-foot-edge-study-v1`: rejected. The left point was inserted on
  the wrong side of the bottom-left corner in contour order, cutting through
  the footboard. Successful capture execution did not mean valid artwork.
- `output/med-bed-foot-edge-study-v2`: corrects that order. Native child 0 with
  no engine/script errors. Bed 0 dark and bed 1 light images were inspected;
  the footboards remain intact and the broad floor wedges are reduced.
  All eight non-bed light/dark renders are byte-identical to the baseline.

Editor import generates the study's paired UID and exits 0 without script errors.
This candidate has not yet passed matched full-room/gameplay-scale review,
retained-hardware pixel checks, card rebaking or four-rotation integration tests.
Do not promote it on the two inspected isolated images alone. The current bed
size, low walls, south-facing rule and selected card remain unchanged.

Next: compare the candidate's entire bed base against the source, quantify the
removed regions, and review both beds in rotated full-room views before adoption.

## Full-room and retention follow-up

`tools/review_med_bed_foot_study.gd` adds matched baseline/candidate views for all
four rotations at 2.0 and 0.7 pixels per world unit. This is renderer-only review,
not an actual gameplay camera or crew test. Its retained-base oracle checks 1500
source-coordinate interior points across both beds, plus four excluded floor
points and unchanged ground footprints. It does not exhaustively classify every
removed boundary pixel.

The first run fails to parse because its helper class used Godot's reserved
`Panel` name. V2 renders but rejects a wedge sample that was already outside the
baseline polygon; correcting that sample from x389 to x388 at y510 resolves the
test setup. V3 then exits 0 with 16 captures and zero failures. The original
bad contour order is exercised by `--negative-footboard-cut`: it exits 1 with
716 retention failures. No production geometry was modified to pass these checks.

V3 candidate q0/q1/q2 at 2x and q3 at 0.7x were visually inspected. The lower-scale
q3 baseline and candidate both expose an existing preparation-area mat outside
the east hull; this is unrelated to the bed contour. Tight framing at 2x hid that
overflow. V4 expands the review canvas to 1000x1040 while retaining the same scales,
exits 0 and produces all sixteen comparisons. Its q3 2x candidate was inspected.
The test's paired UID is generated by a successful editor import.

The bed candidate remains unselected. Next priority is resolving the existing
mat overflow, then integrating the bed cutout with card and runtime follow-up.
Neither passing retention checks nor clean bed edges establish whole-room
containment while that floor detail still extends beyond the hull.

## Integration follow-up

After the separate preparation-mat repair, the two reviewed inset points are now
authored by `med_bay_view.gd` for both beds. The historical study subclass delegates
without inserting them twice. The comparison tool reconstructs the former two
diagonal edges only in its baseline instance, preserving a useful old/new check.

`med-bed-foot-integrated-v1` exits 0: 1500 retained-base points, four excluded wedge
points and unchanged ground footprints pass. All sixteen native comparison PNGs
match `med-mat-contained-review-v1` byte for byte, including all four rotations at
both scales. `med-foot-integrated-negative-v1` rejects the original point-order
mistake with 716 retention failures, child 1.

Current renderer SHA-256:
`4FDE2646ACC28B8AC62E7D08F9C660AF042CC8127D745F3D42155BE2CE3B3E86`.
New selected 512x512 card `med-bay-card-foot-v3.png`, SHA-256:
`C94F5665428993859EFA106CEFAE3FEBF2A2F35B456E5A51CA5175A38524A4A6`.
The native card was visually inspected and its LFS attribute verified. Main card
mapping, grid fallback and whole-room export manifest all select it; earlier cards
remain intact. The 82x39 contained preparation mat is retained.

`med-foot-state-native-v1` exits 0 without engine/script errors: four-rotation
assembly containment, per-host powered/offline effects, socket infill and injected
input isolation pass. Its 808 legacy progress samples remain explicitly separate
from current-controller traversal. Source art, bed size, sort lines, collisions,
low walls and door geometry are unchanged.

Fresh packaged verification remains pending for this integrated revision. The
sampling grid and matched images do not claim exhaustive classification of every
boundary pixel, every actor pose or all Medical Bay decoration.
