# Marsh west ground transitions

Updated September 13, 2026.

## Objective and constraints
Replace abrupt west idle/walk boundaries with authored weight transfer and settling. Preserve left profile, hidden anatomical-right implant, full gloves and no helmet. Do not mirror east artwork or assume east timing/travel fits a different source.

## Current evidence
Native current production baseline passes 155 samples at 30fps in `output/crew-replacement-2026-09-12/marsh/west-ground-baseline-30fps.log`. Captures and reviewed boundary page are preserved in `live-idle-transition-west-baseline-01-fps-30` under that folder. Start jumps directly from planted idle to wide stride; stop jumps from raised trailing foot to planted idle. This is baseline evidence of unfinished visual transitions, not acceptance of those joins.

## Source and extracted candidates
Source completed: `character/marsh-motion-polish-v1/sources/start-stop-west-video-01.mp4`, 1,745,306 bytes, 97 frames, 24fps, 1248x1664. Exact prompt, dedicated start-image binding, hashes and completed provider result retained in `.job.json`. CLI session 88234 completed; no active generation request. Temporal overview and focused start/stop contacts reviewed: facing/identity retained, gradual knee/arm/torso motion and planted final stance.

`start-west-candidate-recipe.json` selects frames 10/16/22/28; stop recipe selects 62/68/74/82. Both use fixed scale 147/1380, position 61/59, canvas 256 square, pivot 128/224, provisional 4x150ms timing. Extraction output is under `review/start-west-video-candidate-01` and corresponding stop folder. `review/west-transition-comparison-01.png` compares all eight poses against selected idle and walk; proportions and endpoint stance reviewed. `west-transition-feet-01.png` gives nearest-neighbor foot closeups. Supporting heel/toe choice and contact height vary through the source; do not infer a travel profile from silhouette bounds or copy east's curve. No runtime selection, motion timing or foot-lock acceptance yet.

## Next action
Calibrate travel and timing from support-foot motion before extending production controller, supplemental contract and native/save checks. Broader crew polish remains active.

## Contact-travel calibration
`character/marsh-motion-polish-v1/west-ground-contact-study.json` records provisional per-pose westward distances: start 0/3/14/26 dense pixels; stop 0/8/12/15. Approximate landmark uncertainty is +/-3 pixels, especially the occluded initial far boot. This corresponds to about 11.47 and 6.62 world units respectively at Marsh's 65.28/148 scale; it is not a final continuous movement curve.

New `tools/review_crew_contact_travel.py` produces fixed-floor-grid contacts and timed GIFs from the explicit study, retaining source and recipe hashes without changing selected assets. Both contacts in `review/west-contact-travel-01` were visually reviewed: support location is approximately retained while the torso translates. `west-transition-foot-grid-01.png` records the source-coordinate closeup used for manual estimates. Preview uses provisional 100ms slots; extraction recipes retain 150ms until a native trial resolves timing. This deliberate distinction is not runtime selection.

Next: implement a west-only native candidate trial using these measurements, check continuous start/walk/stop/idle joins and update-partition travel, then integrate only after review. Production west movement is still unchanged. No active jobs.

## Native candidate trial
`tests/playtest_marsh_walk.gd` now includes an opt-in WestTrial and `west-transition-trial` flag. It loads candidate manifests only inside the fixture; production catalog/controller remain unchanged. Start distance knots are 0/3/14/26/36 dense pixels at 0/.1/.2/.3/.4 seconds (the last segment carries into normal walking); stop knots are 0/8/12/15/15. Four 100ms candidate slots are supplied by local trial manifests. Existing extraction recipes still record the original provisional 150ms study.

`output/crew-replacement-2026-09-12/marsh/west-transition-native-01.log` passes 161 samples at 30fps: 12 walk-start, 50 walk, 12 walk-stop, 87 idle. Both state-transition pages in `live-idle-transition-west-west-trial-01-fps-30` were visually reviewed. Idle/start and stop/idle now use near-matching planted stance; intermediate starts/walk and walk/stop avoid the baseline's direct idle-to-wide-stride change on this route. This is bounded candidate visual evidence, not complete foot-lock or production acceptance.

Next: add update-partition/arrival-time and route-cancellation checks to the west candidate before promotion. The trial currently has no snapshot extension and needs full production interruption/Save handling before integration. Native run and preview generation have completed; no active sessions.

## West interruption checks
Added `west-unit-check` with 1/60, .1, .3 and .7-second partitions of one second of start travel; all reach the expected integrated distance. Zero-delta preservation also passes. First run `west-unit-before-python.log` reproduced six stale-start failures (route replacement/cancellation, death, action, water and blocked movement). WestTrial now validates active pose state both when queried by rendering and before movement. `west-unit-final.log` passes start cases and stop route/cancellation/death/action cases. These checks remain fixture-only; production controller is unchanged. Arrival-timer partition checks, snapshot/Save and production promotion remain outstanding. No active process.

## Arrival-time repair
`west-arrival-before.log` reproduced update-dependent arrival timers on 1/5/10-unit starts and an 8-unit already-walking stop: the largest update retained timer 5.7 while small updates yielded approximately 5.08/5.18/5.28/5.33. WestTrial now inverts the appropriate piecewise travel curve to recover actual arrival time and subtracts the unused remainder from idle. `west-arrival-after.log` passes all four route cases across 1/60, .1, .3 and .7-second updates, alongside prior travel and interruption checks. Exact destination is asserted. This is a candidate timing repair; short-route visual handling and full Save/production promotion remain pending. No active test process.

## Short-route visual failure and source follow-up
Native `west-short-native-01.log` passes 95 captured samples for a five-unit route at 30fps. Reviewed boundary page under `live-idle-transition-west-west-trial-01-fps-30-route-5` shows raised-boot start cut directly to idle. Correct destination/timer therefore does not close short-route visual quality. Candidate must not be promoted as complete locomotion on this evidence.

A dedicated short-step source is now generating: `character/marsh-motion-polish-v1/sources/short-step-west-video-01.prompt.txt`, bound with start_image to reviewed west-standing-identity-01.png. Exact provider identity and hashes are in short-step-west-video-01.job.json; current status in_progress confirmed by provider listing. Active CLI wait session 53237, log `output/crew-replacement-2026-09-12/marsh/short-step-west-generation-01.log`. Resume that request; do not duplicate it on timeout. Prompt requests one small forward step, trailing-boot catch-up, torso translation and planted finish while preserving left profile and hidden right implant. Next: review completed source and extract a measured complete short-step candidate; Save and production integration remain open.

## Snapshot candidate and completed short-step source
WestTrial now stores optional `west_ground` phase/clock/target metadata and validates finite clock range, phase, west-facing dry route coherence and settled destination. Older records without metadata remain valid. `west-snapshot-unit-01.log` passes valid start/settling records and malformed clock/target/facing rejection with existing travel/interruption checks. `west-snapshot-native-01.log` passes 161 samples, forcibly clearing then restoring both start and stop clocks through encoded actor snapshots; position, idle timer, action clock and visual state are asserted. Captures: `live-idle-transition-west-west-trial-01-snapshot-01-fps-30`. This is actor restore in the native fixture, not full disk Save/Continue or fresh visual acceptance of every capture. Production integration remains open.

Short-step source completed and downloaded: `character/marsh-motion-polish-v1/sources/short-step-west-video-01.mp4`, 1,081,777 bytes. Durable job status completed, exact prompt/reference hashes and result retained. Generation session 53237 and native session 59387 are terminal. Next: temporal review and extraction of this short-step source; no active process. Full disk Save/Continue follows eventual controller promotion.

## Short-step source rejection and revision
Source01 overview (97 frames, 24fps, 1248x1664) and focused frames18..62 were reviewed. `review/short-step-west-detail-01/feet.png` shows overlapping boots/shuffling without clear alternating sole lift and trailing catch-up. Rejected in durable job metadata; not extracted or selected. Correct facing alone is insufficient.

Revision02 requests distinct near-boot lift with a visible magenta sole gap, one-boot-length forward plant, then far-boot lift/catch-up while the first stays fixed. Same dedicated west start image. Prompt/job `sources/short-step-west-video-02.*`; provider in_progress confirmed. Active CLI session65192; log `output/crew-replacement-2026-09-12/marsh/short-step-west-generation-02.log`. Resume existing request. No production change; native/save integration remains pending.

## Native pause verification
`west-pause-native-01.log` passes 161 samples after adding explicit paused actor updates during both restored west start and stop. One-second paused updates preserve foot position, idle timer and pose clock; the fixture resumes normal updates afterward. Captures refresh the existing snapshot-trial directory because candidate art/travel is unchanged. This adds pause assertions to the native actor-restore evidence; it remains distinct from full disk Save/Continue. Test session80715 completed. Revision02 generation session65192 remains live on direct polling; resume it without resubmission.

## Selector fix and revision02 rejection
`west-selector-before.log` reproduced missing start when a route selector presets state=walk. WestTrial now overrides the existing route hook to arm eligible west travel and preserves already-active west transitions. `west-selector-after.log` passes with previous travel, cancellation, arrival and snapshot unit checks. Still fixture-only.

Short-step revision02 completed and downloaded (1,101,266 bytes; 97 frames, 24fps, 1248x1664). Overview at `review/short-step-west-video-02/temporal-contact.png` reviewed: visible leading-foot separation and trailing lift improve on01, but frames78..96 remain in a split stance rather than completing catch-up into idle. Rejected in durable job metadata as incomplete-catch-up, not selected. Generation65192 and review44872 completed; no active process. Next source attempt should constrain the ending pose explicitly or use a source with a verified complete planted return; do not trim this split finish into selected idle and call it repaired. Production promotion remains outstanding.

## Explicit ending-pose revision03
Confirmed Seedance schema supports start_image and end_image in omni_reference. Revision03 now binds the reviewed west standing identity to BOTH endpoints and requests a complete stepping-in-place sequence with near lift/plant, trailing catch-up, then 1.5s narrow planted hold. This changes the source approach from free translating motion to a constrained sprite transition; it still must prove weight transfer and compatible travel without sole sliding. Prompt/job `sources/short-step-west-video-03.*` preserve both bindings and endpoint hash. Provider in_progress confirmed; active CLI session45361, log `output/crew-replacement-2026-09-12/marsh/short-step-west-generation-03.log`. Resume existing request. No runtime selection or new acceptance.

West selector reentry verification: `west-selector-reentry.log` passes explicit active-start and active-stop clock preservation when the route hook runs again, alongside existing unit checks. Revision03 remains in_progress in a fresh provider listing and direct session45361 polling; no timeout-based duplicate was submitted. No selected art or production controller changed.

## Revision03 extracted
Revision03 completed, 1,045,411 bytes; overview97 frames/24fps/1248x1664 reviewed. Both endpoint bindings produced a narrow planted ending, unlike02. Seven poses at26/34/43/52/61/69/78 extracted through `short-step-west-candidate-recipe.json` with fixed scale147/1305, position55/55, canvas256 square, pivot128/224, provisional7x120ms. Source standing bounds are500/189/790/1494; registration uses actual1305px source height, not the earlier source's1380px. `review/short-step-west-candidate-03/idle-comparison.png` reviewed: identity/proportions and endpoint stance match selected idle, intermediate leg motion visible. Still unselected: in-place source needs supporting-foot travel calibration, native short-route review, snapshot handling and production integration. Generation45361 and overview23834 complete; no active process.

## Short-step03 native candidate
`short-step-west-contact-study.json` provisionally assigns per-pose travel0/0/0/0/0/10/14 dense pixels, uncertainty+/-3, moving mostly during catch-up. Fixed-grid contact reviewed under `review/short-step-west-contact-travel-03`; source foot closeup under candidate03/foot-contact.png. A WestShortTrial and `west-short-trial` flag in the existing fixture now load the seven120ms poses and exercise5..7-unit west routes (only6 visually checked). Travel starts at.48s, reaches10/14 by.6s, full route by.72s, settles until.84s. This is provisional candidate behavior, not production.

`west-short-native-03.log` passes111 samples on six units at30fps. Both boundaries in `live-idle-transition-west-short-03-fps-30-route-6/state-transition-00.png` reviewed: planted start/end replace the earlier raised-boot return. Candidate still needs arrival-time remainder, interruption, selector-entry and snapshot tests before production promotion; other supported range endpoints not yet reviewed. Do not infer those checks from the separate WestTrial start/stop tests. Sessions82160/47160 completed, no active process.

## Short-step timing and cancellation repair
New `west-short-unit-check` reproduced arrival timers5.7333/5.8/5.9/6.0 instead of5.72 across1/60,.1,.3,.7 updates, plus stale poses after route replacement/cancellation, death and action. `west-short-unit-before.log` preserves failures. WestShortTrial now consumes leftover time after its.72s arrival and validates active target/state in render queries and before movement. `west-short-unit-after.log` passes exact six-unit arrival, expected timer5.72 and all four cancellation cases. Candidate remains unselected; selector-entry, range boundaries, short-step snapshots and production/full Save integration remain outstanding. No active processes.

## Short-step selector and range boundaries
`west-short-selector-before.log` reproduced both5/7-unit routes choosing normal start when state was preset to walk. Centralized `begin_west_short` now serves the route hook and direct move, clears ordinary west phase, and rejects ineligible/dead routes. `west-short-selector-after.log` passes existing short timing/cancellation checks and both selector endpoints. `west-short-range-5.log` and -7 each pass111 native samples at30fps. Both corresponding boundary pages under `live-idle-transition-west-short-03-fps-30-route-{5,7}` reviewed with planted idle joins, complementing prior6-unit review. Candidate still needs short-step snapshots/full Save and production promotion. All test/media sessions completed.

## West short-step snapshots
WestShortTrial now records optional west_short clock/target/length, validates finite ranges and west route/settled target coherence, rejects overlap with west start/stop, and restores the short clock after parent restore. `west-short-snapshot-unit.log` passes moving/settling records, legacy omission and malformed clocks/lengths alongside existing unit checks. `west-short-snapshot-native.log` passes111 samples at30fps on6units, forcibly clearing then restoring short clock during travelling and settling; pose, position, timer and action clock asserted. Captures under `live-idle-transition-west-short-03-step-snapshot-01-fps-30-route-6`. This is encoded actor restore, not disk Save/Continue. Next: production controller/asset promotion with explicit supplemental contract and full disk Save checks. Session86261 complete; no active process.

## Production promotion
Reviewed WestTrial/WestShortTrial logic merged into `scripts/marsh_west_motion.gd` (paired Godot-generated UID), extending existing east ground motion. MarshNPC extends this controller and validates west snapshots before battery fields. Fixture aliases now inherit production Marsh. New supplemental/ground-west manifest contains3states/15frames and is emitted by build_marsh_ground_transitions; frozen supplemental contract includes exact hashes. Art validation passes146body states/706references, zero errors/borders and211original frames unchanged. Ledger814rows; three west motion rows selected with disk-save-pending status.

Production west, west-short, east-start, east-stop and east-short unit checks all pass in `production-*-unit-check.log`. `west-production-native.log` passes111samples on6units at30fps using production-motion with no trial override. Captures `live-idle-transition-west-production-01-fps-30-route-6`; native behavior assertions pass, new capture visual review not yet recorded. Full disk Save/Continue and complete-pack/battery regression remain required before this milestone closes. No active processes.

## West production milestone verified
Extended `tests/test_marsh_battery.gd` full disk Save/Continue cases to west start, stop, short-step travel and settling, using the clear reverse route and reacquiring actors after station rebuild. All cases compare position/timer/action clock/visual state/selected texture pixels and pause/resume. `output/test-runs/20260913-021738-headless` passes complete packs and Marsh battery (116route samples). Production six-unit boundary capture `live-idle-transition-west-production-01-fps-30-route-6/state-transition-00.png` now visually reviewed with planted joins. Three ledger rows advanced to selected-west-motion-native-and-save-reviewed; total814selected clips. All211original Marsh frames remain unchanged per promotion validation. No export or owner acceptance.

Remaining scope: broader autonomous usage, other short-route distances, north/south starts/stops and short steps, older Marsh action/run/carry, Veld carry/seated and Branforth kneel/carry continuity. This closes bounded west production integration, not the overall crew-polish goal. No active process.

## Rebuild provenance repair
`tools/build_marsh_ground_transitions.py` now records west video, extraction recipe and selection-manifest hashes plus extraction/selected durations separately. It asserts canvas/pivot/density/nonloop metadata and correctly reports6states/30frames across east/west (previous return counted east only). Rebuild byte comparison preserves all30selected PNGs and both manifests; art validator passes146states/706references with211original frames unchanged. This changes provenance/reporting only, no motion or selected art. Credits still block new north artwork; existing-source/controller work remains available.

## Production obstacle cancellation fix
New blocked-peer probe reproduced a stopped west short step retaining its pose while the base controller kept a queued route. `scripts/marsh_west_motion.gd` now requires walk state for active short travel with a nonempty path; settled idle at the destination remains allowed. `west-short-obstacle.log` records failure and `west-short-obstacle-after.log` passes the fix with existing short-step checks. Battery/full transition Save regression passes116route samples in `output/test-runs/20260913-022516-headless`. Selected art unchanged; this is a production interruption repair. No active processes.

## Complete-route update remainder fix
20/25-unit west routes exposed different arrival timers across update partitions in `west-complete-route-before.log`. After start completion, remaining update time now re-enters west motion so a stop threshold can be processed within the same update. Dry-west linear fallback preserves idle time after arrival where insufficient distance remains for a full stop; east/other-direction fallback timing is untouched. `west-complete-route-after.log` passes20/25/50/100-unit routes across1/60,.1,.3,.7 updates with existing west checks. Battery/full Save regression passes116samples in output/test-runs/20260913-022733-headless. Very short distances outside authored step ranges still need visual coverage; this is timing correctness, not acceptance of missing poses. No active processes.

## East counterpart timing repair
`east-complete-route-before.log` reproduced east20/25/50-unit arrival timer divergence across1/60,.1,.3,.7 updates. `scripts/marsh_ground_motion.gd` now retires the completed start clock immediately and re-enters ground braking with leftover time, instead of bypassing it through base move. `east-complete-route-after.log` passes20/25/50/100 routes and existing east start checks. Full battery/transition disk Save regression passes116route samples in output/test-runs/20260913-022937-headless. Selected art unchanged; no new visual acceptance claimed. No active processes.
