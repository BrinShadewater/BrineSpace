# Ten-room current verification status

All ten requested room identities have source art, registered station rendering,
operating-state treatment and baked card integration. The manifest now records
`integrated_verified_scopes` and links the current batch evidence. Earlier
individual integration documents describe initial passes; their pending technical
gates are superseded by the later scoped batch reports listed in the manifest.

Verified scopes include three native viewport sizes per room, canonical socket
and collision sweeps, ingress-turn production walker paths, controlled economy
state/frame comparisons, sampled crossing and blocked-wall visual review, a
fifteen-room mixed-station exercise, explicit PCK execution and a real Windows
debug executable with raw-source hash/PNG assertions. Command Center's legacy
perimeter route and missing raw PNG export bytes were corrected after failures.

This does not erase recorded art tradeoffs: source floor within cable loops,
static machinery under operating cues, grouped furniture collision and closed
drone deployment hatches remain as documented. Owner aesthetic acceptance is
separate, as are release signing/distribution and broad hardware compatibility.
No build was published, no commit made, and no player save migration was added.

Use PRODUCTION_TEN_WINDOWS_EXPORT.md for the current build procedure and v7
asset-failure control. Use the per-room records for exact source registration,
and the batch reports for the scope of each test rather than a global PASS claim.

## Actual viewport dimension audit

The saved full frames were checked directly after a later BRINE fixture exposed
a retained window-mode sizing issue. All 30 original-ten room/viewport sets
match 1280x720, 1600x900 or 2560x1440: 29 full frames per set, 870 total.
Square room crops are excluded. Evidence is
`output/production-ten/viewport-dimension-audit.json`. This confirms dimensions
only; it does not expand the original gameplay or visual-review scope. The
shared capture fixture now explicitly selects windowed mode and asserts each
captured image matches its requested dimensions.

## Targeted finishing pass

See BRINE_AND_ROOM_POLISH.md for seven-room cable-opening cleanup, matching cards, shared low horizontal doors and the combined34-identity Windows debug export. Earlier source-loop caveats are narrowed to unreviewed fine margins; explicit reviewed openings are recorded in floor-cutout-review.json.
