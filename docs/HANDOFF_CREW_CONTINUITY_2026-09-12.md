# Crew motion continuity review - September 12

Current selected body frames were compared at common standing density and aligned pivots using tools/review_crew_motion_continuity.py. It produced 24 Marsh and 20 each Veld/Branforth probes under output/crew-replacement-2026-09-12/ACTOR/continuity-review-01/. Source paths and scale/pivot evidence are in each review.json. These are pose comparisons, not live transition acceptance.

Observed: Marsh old idle/action endpoints have visibly different head/body proportions and coarse shading from the new walk. His old run and carry side poses retain awkward bent cutout legs. Veld carry differs in suit color/face from corrected idle and new walk. Branforth older kneel north/south/west poses differ in density, costume and face from the new walk; carry also retains old leg treatment. Run is absent from Veld/Branforth selected catalogs; this is not yet established as a runtime defect because consumers may use walk.

Priority: replace Marsh idle in matching identity/style, then assess its actual stop/start transitions; follow with run/carry and the other actors' mismatched action states. Preserve all 20 reviewed walk selections. No claim that the full 808-clip replacement is complete.

Marsh east idle video source is in progress using reviewed east standing reference, exact prompt and durable job record under character/marsh-motion-polish-v1/sources/idle-east-video-01.*. Local waiting session 98054; log output/crew-replacement-2026-09-12/marsh/idle-east-video-generation-01.log. Resume the same job. Existing idle contract has two 650-ms frames; source inspection and registration precede selection. No idle runtime replacement yet.

West idle source is also submitted from the reviewed west reference (clear anatomical-left temple); durable job sources/idle-west-video-01.job.json, waiting session 81296, log idle-west-video-generation-01.log. Preserve the exact queued job. The comparison helper now writes source SHA-256 hashes for new reports; existing contact reports predate that addition. Pipeline reference synchronized. No idle selection yet.

East idle milestone: source completed/downloaded (721,937 bytes), 97 frames at 24 fps, 1248x1664. Source overview reviewed: planted feet and subtle breathing. Recipe idle-east-cycle-recipe.json selects frames 8/43 with fixed scale 147/1360, position [61,55], original two 650-ms holds. Explicit shared override selected; rebuild and asset validator pass. Native candidate flag marsh-idle-east verifies exact anchored source pixels and captures idle/walk comparison; exit 0. continuity-review-02 contact inspected and shows improved east idle/walk identity consistency. Ledger records selected-clip-reviewed-live-context-pending. Live stop/start still required; remaining idle directions and actions unchanged. West job still active in session 81296; do not resubmit.

West idle milestone: completed source downloaded (717,236 bytes), 97 frames/24 fps/1248x1664. Overview and registered contact reviewed; frames 43/61, scale 147/1468, position [63,66], two 650-ms holds. Explicit shared override selected. Asset validator and native anchored-source checks pass. continuity-review-03 inspected: both side idles now match new walking identity/style. Ledger marks both selected-clip-reviewed-live-context-pending. No provider jobs remain active. Next: real side idle/walk transitions, then north/south idle and older action/run/carry discontinuities.

Live side continuity milestone: tests/playtest_marsh_walk.gd now supports idle-transition, a controlled clear 100-unit route using real NPC update/move/arrive and renderer clocks. Each side passed 51 samples with full idle cycles before/after travel, planted idle feet and real battery drain. Logs idle-transition-{east,west}-live.log; raw traces and reviewed contacts/GIFs in live-idle-transition-{east,west}/. East transition-boundaries.png isolates indices 14/15 and 35/36. Identity consistency is improved, but direct start on a wide-stride pose is visibly abrupt; short authored start/stop transition work remains. This is controlled runtime evidence, not autonomous goal-selection coverage. Both idle ledger rows now explicitly retain transition polish pending. No active jobs.

Start/stop follow-up: source study submitted from the reviewed Marsh east standing reference. Exact prompt sources/start-stop-east-video-01.prompt.txt; durable job start-stop-east-video-01.job.json; waiting session 59120; log start-stop-east-video-generation-01.log. In progress, not selected. It requests planted idle, weight shift into two steps, then planted idle. Resume this job without duplication.

Runtime findings: scripts/crew_sprite_player.gd resets a new walking key to phase zero; its transition hook currently covers water/carry only. Companion move-start/move-stop clips and speed ramp are implemented in scripts/companion_motion.gd and tools/build_animation_expansion.py, not the crew catalogs. New crew transition behavior must be explicitly enabled by actual reviewed assets and preserve Bill/other absent-clip behavior. Source endpoints must join selected idle/walk poses; rendering a held idle while the physical foot translates would introduce sliding, so test both visual pose and world travel. New starts/stops will need explicit supplemental coverage rather than rewriting frozen source contracts. No runtime behavior change made yet.

Start/stop source completed and downloaded (1,593,612 bytes; 97 frames, 24 fps, 1248x1664). Durable job now terminal completed; no active sessions. Overview and dense 8-32 contact inspected. It includes standing, gradual initial weight shift/step, and final planted idle. Unselected start-east-candidate-recipe.json extracts frames 14/18/22/26 at scale 147/1300, position [61,54], provisional 100-ms holds. Candidate contact reviewed. Next: match its endpoint to selected walk and calibrate physical travel before opt-in runtime integration; stop segment is not yet extracted.

Pipeline: review_crew_live_walk.py now creates paged adjacent-frame state/direction boundary contacts and records exact source pairs. Verified against west 51-sample trace; pairs 14/15 and 35/36 clearly expose the entry/exit changes. Raw captures remain unchanged.

Start candidate calibration: endpoint-comparison.png compares selected idle, four start poses and all six selected walk poses at common pivot. support-foot-landmarks.png reviewed. Approximate support heel x=[115,115,110,101], uncertainty ~3 dense pixels including toe-off rotation. Uniform travel [0,9,18,27] produces a 13-pixel boundary residual; fitted travel [0,0,5,14] preserves those manually measured boundary positions by construction. This is calibration, not independent validation or continuous foot-lock proof. support-foot-analysis.json and support-matched-travel.gif preserve the study. Initial weight shift requires little/no translation before acceleration; a render-only start clip at full movement speed would slide. Next: opt-in physical start profile and phase bridge into selected walk, with native/runtime checks, followed by stop transition. No runtime behavior changed or new ledger acceptance.

Runtime start trial: tests/playtest_marsh_walk.gd has an inner StartTrial subclass, enabled only by idle-transition start-trial. It uses real Marsh move/arrival/collision code with integrated early travel and renders supplemental trial-manifest.json via existing action-time rendering. Production scripts/catalogs remain unchanged. Native run exited 0 with 54 samples, all four start poses, real battery/idle-foot checks. Trace verification measured dense travel [0,0,5.000365,14.000358] versus [0,0,5,14]. Reviewed state-transition-00.png shows gradual entry; stop remains abrupt. Evidence: output/crew-replacement-2026-09-12/marsh/start-trial-east-live.log and live-idle-transition-east-start-trial-01/ including trial-verification.json. Scope is 100-ms controlled fixture only; variable-delta integration, facing/collision interrupts, save/restore and stop transition remain before production promotion. No active processes.

Start trial revision 02: fixed discarded delta beyond the 0.4s start boundary and the one-update pose lag. Source-distance knots now align to pose times: 0/0/5/14/24 dense pixels at 0/.1/.2/.3/.4s, then ordinary movement consumes any remaining update time. Unit probe in the same fixture passes 1/60, .1, .3 and .7s update partitions over one second, zero delta, east-to-west reversal and blocked-step cancellation (start-trial-unit-python.log, exit 0). Original 01 visual evidence remains preserved. Revision 02 native rerun is active in session 71200, log start-trial-east-live-02.log; inspect terminal result before review. Production promotion, stop motion and snapshot semantics remain open.

Revision 02 live rerun completed, exit 0, 53 samples; overview and boundary contact reviewed in live-idle-transition-east-start-trial-02/. Entry remains gradual; stop is still direct to idle. No active processes. Unit scope uses a clear-path probe for delta/interruption logic, complemented by native real-collision fixture; it is not a full gameplay acceptance.

Stop candidate: reviewed dense source 60-88, extracted frames 66/70/74/78 using shared start registration (scale 147/1300, position [61,54]), provisional 100-ms holds. Recipe stop-east-candidate-recipe.json and review/stop-east-video-candidate-01 preserve contact, support-foot crop and idle endpoint comparison. Final pose matches selected idle closely in identity/proportions. Approximate support heels [132,123,118,115] imply fitted travel [0,9,14,17] dense pixels, then zero-travel settling; uncertainty ~3px. No independent foot-lock claim. Runtime trial must initiate before arrival, preserve endpoint/arrival semantics and evaluate varying gait entry phases and route lengths. Stop is not yet enabled.

Combined stop trial 01: fixture-only StopTrial extends StartTrial, activates on the final clear eastward segment with empty goal/stage, reserves 17 dense pixels of remaining travel, and settles through the four source poses. Existing move/arrive handles target and collision. Native 100-unit route passed 54 samples: 29 idle, 3 start, 18 walk, 4 stop; final foot exactly [7812,7744]. Boundary pages 00/01 reviewed, including stop-to-selected-idle join. Production remains unchanged. Added route-length review option and guard after approach collision before accessing path target. Route 90/110 native cases are running in session 6619, logs stop-trial-east-route-{90,110}.log. Stop variable-delta/settling-time, interruption, arrival-action and snapshot semantics remain unproven; no promotion or ledger acceptance.

Stop route variation completed: 90-unit route exit 0, 52 samples; 110-unit route exit 0, 56 samples. Both review media generated and transition contacts inspected, including 110 stop-boundary-closeup.png. Alongside the 100-unit route this gives three distinct gait entry timings with exact target arrival and final planted idle. It does not establish all gait phases or interruption/delta correctness. No active sessions. Next: stop timing and cancellation probes, arrival actions and snapshot handling, then selective production promotion.

September 13 cancellation checks: new StopProbe reaches the destination exactly once under 1/60/.1/.3/.7s partitions; zero delta, reversal and blocked-stop checks pass. It initially failed arrival-action and death precedence (stop-trial-unit-before.log, exit 1). StartTrial/StopTrial now invalidate pose overrides for death, action/stage changes, non-dry medium and non-east facing. Both start/stop unit suites pass in *-trial-unit-action-cancel.log. These are fixture-only fixes, with no production behavior changes. Inspected base and Marsh snapshot methods: trial clocks/stop target are absent, so snapshot validation and restore remain required. Stop settling timer consumption across arrival is also not fully verified by endpoint-only probes. No active processes.

September 13 arrival-time accounting: added an analytical timer check over one second using 1/60/.1/.3/.7s update partitions. Before fix, the six-second arrival idle retained 5.3333/5.4/5.6/5.7s respectively (stop-trial-arrival-time-before.log, exit 1). StopTrial now accounts for elapsed time after the .3s distance plateau in the same update, including remainder beyond the .4s settling clip. All stop unit checks pass in stop-trial-arrival-time-after.log, exit 0. This fixture-only timing correction does not change art or production behavior; previous native visuals remain relevant to poses, not proof of this timer calculation. Snapshot support and production promotion remain open.

September 13 snapshot trial: StartTrial and StopTrial now serialize and validate their transition clocks and finite stop target, restore through the canonical Marsh snapshot path, and cancel incompatible restored states. Native encoded actor snapshot round trips during both walk-start and walk-stop passed, preserving foot position, timer and animation state: start-stop-snapshot-live.log, 54 samples. Review media generated in live-idle-transition-east-stop-trial-01-snapshot-01; stop-to-idle boundary inspected. This is an actor round trip on a controlled dry east route, not full save-game acceptance. Malformed metadata coverage and production promotion remain open; production catalogs and gameplay behavior are unchanged by this trial.

September 13 transition snapshot validation: rejection probes exposed accepted inconsistent pose/travel clocks, flooded active transitions, mismatched stop targets and overlapping start/stop. Fixture validators now enforce those relationships and accept legacy snapshots without trial metadata. snapshot-validation-before.log preserves failure; snapshot/start/stop-validation-after.log all pass. Native valid encoded round trips still pass after tightening validation (snapshot-validation-native-after.log, exit 0, 54 samples). Changed tests/playtest_marsh_walk.gd, visual bible and maintained/installed source-density skill reference. Production promotion, full save integration and other directions remain open. No active test or generation session from this work.

September 13 production milestone supersedes the fixture-only status above: east start/empty-goal stop now use the normal Marsh controller and catalog. Current files, scope and checks are reconciled in HANDOFF_MARSH_GROUND_TRANSITIONS_2026-09-13.md. Other directions, full active-transition Save/Continue and broader continuity remain open.
