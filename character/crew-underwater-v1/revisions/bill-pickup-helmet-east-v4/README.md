# Bill pickup revision 4

Provisionally selected through the composed locker manifests. Rebuild with `python character/crew-underwater-v1/build_pickup_revision.py --actor bill --candidate 07`, then rebuild deposit, composition and both reviews. The original donning first figure was used as the sole proportion reference; chunky torso/head proportions are closer at the join in static game-resolution comparison. A smaller helmet-position/shape change remains. Source, prompt, registration and exact donning endpoint are preserved.

All source/manifest checks and twelve-phase timing tests pass. Native captures from before this revision are historical for Bill's pickup/deposit appearance; continuous visual acceptance is open. First empty-hand pose now holds hands in front of the belt, so the idle-to-pickup transition also needs native review.
