# Branforth pickup revision 2

Provisionally selected by the composed locker runtime. Rebuild with `python character/crew-underwater-v1/build_pickup_revision.py --actor branforth --candidate 02`, then rebuild deposit, composed manifests and reviews. Candidate 02 revises the inward approach to lower the helmet toward the existing donning start. Five generated poses lead into the exact existing boundary image. Source/prompt and measured registration are preserved.

Static game-resolution comparison shows a closer helmet height in the penultimate pose than v1. Body and helmet silhouette differences remain; this is not continuous motion acceptance. The derived deposit now reuses this revision too. Source hashes, manifests and all twelve phase timing checks pass (`branforth-pickup-v2-pack.log`). Native captures made before this change are historical for Branforth's pickup/deposit poses and must not be used as evidence of this revision's appearance.
