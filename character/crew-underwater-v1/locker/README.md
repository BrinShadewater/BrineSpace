# Locker animation sequences

Runtime equipment actions load composed manifests for Bill, Veld and Branforth. Each fitting sequence contains pickup then donning (12 frames, 2140 ms); each return contains removal then deposit (12 frames, 2240 ms). Source files are referenced and hashed. Rebuild in order: `build_pickup_revision.py` for the selected actors/candidates, `build_deposit_revision.py`, `build_locker_sequences.py`, `build_locker_review.py`, then `build_review.py`.

The identical boundary images previously inherited two full holds. Composition now reduces that combined hold from 340 to 180 ms for fitting and from 380 to 220 ms for return. Source timings remain preserved; `joinTiming` records each override. Events are recalculated from the final duration list. Pickup grasp remains at 320 ms; deposit release is now at 1900 ms.

The existing NPC action timer, reservation and snapshot mechanism covers the whole sequence. Equipment commits on whole-action completion; interruption keeps the prior gear state. Shelf visibility follows named manifest events and the saved timer. Idle lockers show a staged spare under the existing reusable-gear behavior.

The screen-facing shelf attachment uses the generated east helmet overlay. Native captures in `output/airlock-review-107648` show the corrected attachment with a supporting ledge after lowering the initial placement by 12 station units. Bill, Veld and Branforth release captures were inspected. These static captures predate the shortened join holds, which do not change placement.

Current checks: `revisions/locker-join-airlock.log` passes the production fixture across three actors/four rotations, pause, power interruption, handoff boundaries and disk saves. `locker-join-crew_medium.log` passes cancellation during deposit. `locker-join-crew_death_pack.log` passes twelve-pose timing and terminal holds. Room image-loading warnings remain outside these assertions.

Still provisional: the shared shelf overlay differs from generated held helmets; Bill/Branforth held-pose proportions change at stage joins. Continuous native playback and visual acceptance remain open. Passing frame/timer tests does not close those findings. Standalone stage source READMEs retain historical candidate notes; composed manifests govern current runtime selection and timing.

Save verification expanded: `tests/test_crew_death_save.gd` now covers pickup/removal at 550 ms, donning at 1400 ms, and deposit after release at 1950 ms for all three actors. It checks exact restored action pixels/timer/equipment, interruption, rejection of contradictory gear and eventual completion. Updated the fixture to await the current asynchronous restore API. `revisions/locker-late-save.log` passes (dry traffic minimum separation 20.36). The real airlock fixture additionally writes/restores immediately after release for each actor and checks the unchanged action timer plus visible shelf helmet; see `revisions/locker-release-save.log`.

Run `python character/crew-underwater-v1/check_locker_sequences.py` after composition changes. It checks all six expected packs, source path/hash correspondence, dimensions, pivots, boundary equality, timing overrides and named event phase times without regenerating art. All six pass in the current revision; visual quality remains a separate review.

Full-phase native sampling: `tests/playtest_crew_water.gd` now reads composed runtime durations and samples every one of the twelve phase interiors per action. It asserts all actors share the capture timing, preserves simulation-clock checks and awaits the current snapshot restore API. The native run passes (`revisions/locker-full-phase-native.log`). Exactly 24 current action samples, each showing three crew, are isolated with hashes in `revisions/locker-full-phase-captures/evidence.json`. The old fixed capture folder contained historical images, so only manifest-derived expected files were copied; future fixture runs use process-specific folders. These are still ordered samples, not continuous visual acceptance.

Branforth now selects pickup v2 / generated candidate 02, rebuilt with `--actor branforth --candidate 02`. The final inward poses lower the helmet toward the existing donning start; static contact comparison is closer than v1. Deposit and both reviews were rebuilt from the new source. Runtime hash/registration/event checks and full pose timing tests pass. Older native Branforth pickup/deposit captures are superseded for visual evidence; current native motion remains to be reviewed. Bill and Veld source selection is unchanged.

Current source-native verification: `tests/playtest_crew_water.gd` now compares actual renderer bytes and pivots with the manifest-selected source at each of 72 actor/action phase interiors. Run `pilot/native/water-82196` passes; `locker-evidence.json` there hashes current manifests and 24 three-crew captures. Compared pickup samples at 750/880 ms: Branforth's revised approach is closer with residual helmet-shape change; Bill's body/held-helmet join remains more noticeable. Log: `revisions/locker-current-source-native.log`. This proves current texture selection, not continuous visual acceptance.

Bill now selects pickup v4 / candidate 07 (`--actor bill --candidate 07`). Static comparison shows closer torso/head proportions against the donning source; a helmet-position change and idle-to-first-pose transition remain for review. Derived deposit and both review consumers were rebuilt. Current source/manifest/phase checks pass (`bill-pickup-v4-pack.log`). Existing native Bill pickup/deposit captures predate this revision.

Rebuild entry point: `python character/crew-underwater-v1/rebuild_locker_pipeline.py` rebuilds current actor-specific pickup selections, derived deposit, composed manifests, source/timing checks and both reviews in dependency order. It uses saved artwork and stops on a failed stage. It does not regenerate art, run native tests or confer visual acceptance. Pickup defaults now select Bill candidate 07/v4, Veld 01/v1 and Branforth 02/v2; unsupported explicit actor/source pairings fail instead of silently choosing a different source. Rebuilding the defaults reproduced all 18 selected pickup frames byte-for-byte.

Outer state joins are now included in `revisions/locker-review.html`: one real bare idle cycle before pickup, equipped idle between fitting/return, and bare idle after deposit. `revisions/locker-idle-boundaries.png` compares actual source frames at a shared foot anchor. Static findings: Bill pickup body is bulkier than bare idle; Veld's pickup head/torso shifts relative to idle; Branforth pickup broadens shoulders. Veld and Branforth donning helmets also differ visibly from their shared-overlay equipped idle. These are additional entry/exit defects, not pivot-only issues. Review timeline uses source idle timings and no fabricated held-pose dwell. Continuous motion acceptance remains open.

Dry east helmet registration revision: `fit_dry_helmet.py` now shifts the existing overlay two pixels left after per-pose placement for idle/walk/run/interact/kneel/repair/stand. This exposes more of the actual face while retaining the shared shell design; four-pixel trial was too far back. All available dry east clips were rebuilt for all three actors (Veld/Branforth still have no base run). Placement study: `revisions/east-idle-helmet-placement-study.png`. Both reviews rebuilt. Death/locker phase pack test passes but does not certify dry-helmet appearance; native movement, lowered-pose contact and turning continuity remain to review. Prior dry-east native images are historical for registration.

Dry east native follow-up: `tests/playtest_crew_water.gd` now samples the union of per-actor phase interiors for idle/kneel/repair/stand/interact and compares actual renderer pixels/pivots with fitted source manifests. Native run `pilot/native/water-79992` passes (`revisions/dry-east-full-native.log`); final kneeling sample at 900 ms was inspected at station scale. Walk contact-sheet phases were inspected, but native locomotion/turning continuity remains separate. Ordered phase sampling does not establish continuous motion acceptance.

Dry east walk verification: the native fixture advances each crew member by its own manifest stride fraction rather than using wall-clock time alone. It samples all phase interiors, checks each sampled segment against room geometry and compares renderer pixels with the current helmeted walk source. `revisions/dry-east-walk-native.log` passes; captures are in `pilot/native/water-98504`. Inspected the 235 ms stride sample at station scale. This verifies a short synthetic straight walk, not autonomous routing, turning or continuous visual acceptance.

Locomotion turning refinement: shared crew playback and Bill's separate animation clock now preserve normalized walk/swim phase during facing-only changes, then add the new traveled distance. Action changes, teleports over half a cell and time rewinds still reset. `tests/test_crew_swim_packs.gd` exercises actual three-actor walk/swim timing, both clock implementations, repeated sampling, teleport and rewind; `revisions/locomotion-turn-clock.log` reports zero failures. This removes the forced restart-to-first-frame behavior; native directional silhouette and continuous turning appearance remain to review.

Native dry turning check: `tests/playtest_crew_water.gd` now turns all three helmeted walkers through south/west/north/east at the existing gait fraction and compares actual rendered frames to directional manifest expectations. `revisions/dry-turn-native.log` passes. The four-facing comparison `pilot/native/water-93580/turn-comparison.png` was inspected: helmet placement remains over the heads across views without an obvious scale jump in these samples. This is a stationary facing-change test after a short stride, not continuous path-corner movement; water turning and continuous visual review remain separate.
