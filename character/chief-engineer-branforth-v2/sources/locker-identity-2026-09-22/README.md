# Branforth locker identity source

One built-in image-generation call, September 22, 2026; no Higgsfield.
prompt.txt preserves the exact brief. generated-source.png is untouched.
registration.json records source hash, one body scale, foot registration,
frame ordering and three masks removing erroneous faces inside empty helmets.
The masks replace only the visor interiors with dark lining before extraction.

Canonical builder: tools/build_branforth_locker_identity.py, called by
tools/rebuild_human_crew_art.py branforth --equipment. Canonical idle images supply
the palette and exact padded endpoints. Existing frame timing/events are retained.
build_study.py is the historical output-only extraction script; its recipe status
describes that study, not current installation. Runtime acceptance is recorded in
docs/BRANFORTH_LOCKER_IDENTITY_INTEGRATION_2026-09-22.md.
