# Sidebar hardware row mockup

Built-in image_gen design mockup using the existing heavy lever, vertical comms unit and industrial switch atlas as references. One horizontal row, labels below.

Intended mappings: Power lever toggles power; Comms button opens the comms window; separate slide toggles control interior/exterior lights; recessed pushbutton toggles pumps when that feature exists. Pumps are explicitly not implemented. This is an enlarged design detail, not a validated fit inside the current narrow sidebar and not live control integration. No gameplay behavior changed.

The image redraws the source hardware to present the design; it is not a production texture atlas. Exact prompt is in prompt.txt.
