# Empty cargo dolly handoff

Updated September 8, 2026. Complementary state artwork for ongoing prop production.

## Objective and constraints

Remove the parcel while preserving a recognizable loaded/empty dolly family. Keep the matte deck, four wheels, corner guards and bottom handle at modest36-unit width.

## Current state

`cargo-dolly-empty.png` is a transparent1081x1455 export. Original edit source and exact built-in imagegen prompt remain alongside hashes and review records. [states.json](../cargo-dolly-v1/states.json) links both candidates and proposes shared source region186,199,710,1031 for future display.

## Verification

RGB checkerboard excluded at neutral185/spread22 plus handle seed530,1100. Native Godot checks pass with no script errors; dark/light review retains empty deck, wheels and handle. Handle sample is transparent and deck sample remains near-opaque. Evidence: `output/cargo-dolly-empty-review.png` and matching log. Existing reference-room raw-image packaging warnings remain.

Canvas matches loaded artwork, but registered width grows1 source pixel and height3 pixels. This drift is recorded rather than asserting pixel-exact alignment. Both standalone previews use36-unit width; no animated transition has been tested. Gallery rebuilt with matching export hashes.

## Next action

Owner review, shared-frame runtime state comparison and actual room clearance before integration. No hauling or transition behavior installed. Continue wall and prop production.
