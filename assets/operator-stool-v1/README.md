# Operator stool

## Objective and current state

Small matte charcoal-fabric stool with olive frame, matching the analog control bank. Built-in image generation; exact prompt in `source.prompt.txt`, untouched source in `source.png`. Transparent `operator-stool.png` uses its original 1402×1122 canvas and registered silhouette.

## Verification

Godot export/hash check passed. Agent reviewed dark/light grounds and native22-unit scale beside established furniture. Footrest hole alpha0 and retained seat alpha253 verified. See `../../output/operator-stool-review.png` and `../../output/operator-stool-group.png`.

`group.json` and `tools/review_prop_group.gd` provide a reusable static pair diagnostic with exact export hashes and shared world units. The stool is positioned below the console at its keyboard bay. This is a proposed arrangement, not room integration or occupied clearance evidence.

## Next

Owner review and actual room placement remain pending. Preserve footrest and operator access space when integrating. No gameplay behavior changed.
