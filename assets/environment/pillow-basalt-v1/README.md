# Pillow basalt

One original transparent source containing three fused unequal rounded lobes.
Exact prompt and immutable source hash are preserved. No image processing was applied.

The native 1600 x 900 sheet compares 0.35, 0.45 and 0.55 cell canvases on shell
sediment and volcanic ash, with existing fractured and porous rock references.
The rounded silhouette reads on pale sediment; dark ash suppresses its boundary.
The station uses 0.45 and 0.35-cell canvases on pale ground. This is decorative geology with
no occupancy or removal behavior. Native integration and packaged verification pass; owner review remains pending.

Evidence: `output/pillow-basalt-source-audit-v1.json`,
`output/pillow-basalt-native-v1.log`, and the native preview in this catalogue.

## Integration and export

Two static outcrops at (9.2, 15.2) and (10.2, 15.7) sit below room geometry. Native fixture `output/pillow-basalt-station-v1.log` passes three resolutions with unchanged occupancy/resources; its 1600 capture was inspected. Source registry audit v2 passes. Isolated Windows export v20 passes 91 exact PNGs, 20 packs and 64 runtime textures across ten habitats. The dedicated pillow-basalt capture was inspected.
