Tiny targeted edit to this exact two-module sheet. On the RIGHT module ONLY, its bottom row of THREE cargo bins has handles on lower/south edges. Move each of those three handles to the UPPER/north edge of its same lid, so all bin handles on the entire sheet face UP toward crew. Remove the old lower handles cleanly. Keep every other pixel/design feature as close as possible: both module shapes, white margins, rails, left cargo, upper six bins, console controls ABOVE screen, dark steel/ochre palette, directly overhead view.
