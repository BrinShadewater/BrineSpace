# Side-wall installations, September 8

Nine generated paired sheets provide eighteen registered west/east views for
existing room installations. `generation.json` preserves exact prompts, source
hashes, actual dimensions, direction selection and integration status. Material
references were the corresponding approved horizontal source (refined maintenance);
the mycelium sheet served as the geometry reference for the following eight.

These are RGB originals, not transparent exports. Rendering uses reviewed vector
cutouts under `rooms/full-wall-v1/registrations/side-*.json`. Raw pixels are retained.
The lounge halves were selected in reverse order after inspecting the backing.
The mining cradle is empty so the existing live vehicle remains a separate object.

Godot review and gameplay route evidence: `output/room-polish-2026-09-08/`.
Current selection and cutout ledger: `rooms/full-wall-v1/cutout-review.json`.
