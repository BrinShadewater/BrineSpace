# Medical, Clone and new Shield south artwork

September 12, 2026. Exact initial prompts and reference roles are in `briefs.json`;
subsequent exact prompts are adjacent text files. Three rejected intermediate
images are preserved. Six generated candidates, three registered selections.

Medical and Clone Lab replace their existing south registrations; prior files and
original rasters are preserved. Their source-region frame ratios remain unchanged
so rendering a shallower overhead view does not alter furnishing fallback.
Medical retains cream/teal composites and orange mattress. Clone uses three lid
windows instead of glowing upright vessels, inward case catches, and deliberately
removes two incidental tweezers after the requested rotation failed twice.

Shield adds a previously absent `side-shield-pressure-wall-south.json`; its console
controls and three lever grips are on the north/operator edge. This is available
as a registered library asset. Default host placement is not yet accepted.

Read-only neutral polygon extraction: threshold 228, channel spread 22, source RGB
pixels unchanged. Native medical/clone floor review passes; six unaffected room
rotations are RGB-identical. Preferred layouts: 176 crew-route cases pass. Existing
side-wall tests, 47 card bindings and 188 layout keys pass. These existing tests
do not certify the new Shield host placement. PNG LFS filters and source hashes
checked. Evidence lives in `output/room-facing-batch3-2026-09-12/`.

The broader active scope is `docs/ROOM_FACING_ROLLOUT_2026-09-12.md`; no complete
catalog acceptance, executable export, commit or publication is claimed.
