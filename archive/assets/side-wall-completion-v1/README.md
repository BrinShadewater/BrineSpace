# Full-wall vertical completion

Six newly drawn paired sheets provide twelve west/east variants: Research, Medical, Pressure Control, Listening Post, Xeno and Isolation. Exact prompts and immutable PNG hashes are in the manifest. All sheets are RGB; their white/checkerboard exterior is excluded through read-only vector polygons. They are not alpha PNGs. Native room review checked both facing directions and background exclusion.

The runtime selects these side views for vertical orientations with a sealed side wall. All twelve are also available in Room Layout Studio's registered asset library. Pressure and Listening retain their existing horizontal behavior and use references from the accepted original machinery, not rejected bright replacements. Existing horizontal cards remain current because q0 did not change; the review writes diagnostic cards under output.

All 15 full-wall rooms passed four rotations/two states, installation bounds, door lanes and cryo behavior. All six affected rooms passed production crew routes through all four rotations. The isolated regression verifies twelve hashes/library entries and safe fallback for invalid old draft positions/sizes without rewriting saves. Personal Research furniture overlap remains a separate draft issue.
