# Room style polish v1

Twelve south-facing bank surface/construction-detail repaints. `generation.json` preserves prompts, raw tool paths, department briefs and north reference paths. Raw source files stay intact; runtime registrations use vector exterior exclusion. Previous registrations are backed up here.

The Tidal south source supplies material quality; each family's north source supplies departmental identity. Neither replaces the selected south camera and silhouette. Current runtime registrations retain their proportional placement frames. Native alpha exports under `exports/` are generated by Godot's polygon renderer, not by repainting source pixels.

Per-asset `*-review.json` files record hashes, references, dimensions, probes and findings. Owner acceptance is unset. Native comparisons and the full room review are in `output/room-style-polish-2026-09-08`; project handoff: `docs/ROOM_STYLE_POLISH_2026-09-08.md`.
