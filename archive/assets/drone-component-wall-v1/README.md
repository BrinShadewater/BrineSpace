# Drone component service wall

September 8, 2026. Detached gripper jaws, empty four-point assembly cradle,
supported actuator and diagnostics/cables on a continuous low bank.
`wall.png` is a2172×724 true-alpha canvas, visible320×79.49 world units.

Built-in imagegen sources and exact prompts are preserved. V1 copied battery-pack
shapes from the construction reference; V2 replaces those with angular gripper
jaws and reduces actuator bands. Neutral180 registration clears opaque checkerboard.
The cradle and jaw openings have solid backing and intentionally remain opaque.

Native matte/scale and light/dark alpha review pass:
`output/drone-component-wall-v1/wall-scale.png` and `output/drone-component-wall.log`.
Hashes, reference roles and rejected-source reasons are in the standardized record.

Standalone, not installed or owner accepted. Preserve lift/service routes and
operator access during placement. The empty cradle does not implement repair or
an occupied drone state; an occupied asset/animation would need separate review.
