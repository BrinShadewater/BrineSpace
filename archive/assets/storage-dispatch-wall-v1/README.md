# Storage dispatch wall and cargo dolly

September 8, 2026. Standalone Logistics furniture candidates, generated with the
built-in imagegen tool. No runtime integration or owner acceptance yet.

## Deliverables

- `wall.png`: 2172×724 true-alpha canvas; visible 320×66.73 world units.
  Packing pads/cases, recessed scale, clear wrapping top, paper holder,
  tie-down trays and dispatch drawers.
- `dolly.png`: 1254×1254 true-alpha canvas; visible 38×36.41 world units.
  One strapped case on a low rubber platform with a recessed grab slot.

Sources, exact `*.prompt.txt` briefs, registrations and separate review records
are preserved here. `manifest.json` indexes the selected pair and rejected dolly
V1. V2 replaces reflective yellow bumper streaks and bright case corner diamonds
with dull ochre powder coat and restrained blue-grey molded edges. This edit
returned an opaque checkerboard, requiring fresh registration at neutral180 and
a reviewed aperture seed625,380. Wall white-background registration uses228.

## Verification and next work

Native Godot material/scale boards and logs in `output/storage-dispatch-wall-v1/`
and `output/storage-dispatch-{wall,dolly}.log` pass and were visually inspected.
Transparent exterior corners, dolly grab-slot alpha0 and retained cargo alpha255
were checked. Source hashes and actual canvas dimensions are recorded.

Placement must preserve door bays, loading space and operator access to the
working surface. Depicted weighing and dispatch do not implement gameplay.
The clear wrapping area is intentional and should remain clear when dressing.
