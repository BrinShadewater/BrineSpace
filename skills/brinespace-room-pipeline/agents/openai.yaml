interface:
  display_name: "BrineSpace Room Pipeline"
  short_description: "Create and validate consistent BrineSpace room art"
  default_prompt: "Use $brinespace-room-pipeline to continue the BrineSpace room set with geometry checks and in-game verification."
